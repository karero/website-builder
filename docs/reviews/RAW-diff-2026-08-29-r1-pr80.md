# Raw reviewer output — DIFF gate round 1 (PR #80)

Captured verbatim at run time (streamed to disk). Reviewers: Codex `gpt-5.6-sol` (`exec -s read-only`), ollama-cloud `glm-5.3-flash:cloud` (text-only; includes the model's streamed thinking). Artifact: full branch diff at head fa17fea, docs/reviews/ excluded.

## Independent review — codex (gpt-5.6-sol, read-only)

## Ranked findings

1. **BUG** — [skills/independent-review/SKILL.md:9](/Users/matt/Devel/website-builder-ir-concise/skills/independent-review/SKILL.md:9) — The shortened frontmatter allows “every finding” to be owner-waived, contradicting Procedure step 5, where BUGs are never waivable. **Fix:** restore the distinction: BUGs must be fixed/refuted; only RISK/NIT may be owner-waived.

2. **BUG** — [independent_review.sh:105](/Users/matt/Devel/website-builder-ir-concise/skills/independent-review/scripts/independent_review.sh:105), [setup-guide.md:148](/Users/matt/Devel/website-builder-ir-concise/skills/independent-review/references/setup-guide.md:148) — `gpt-oss:120b` is recommended as local but `is_cloud_ollama_tag` classifies every `*:120b` as cloud, so `--local-only` rejects it and normal mode can wrongly treat it as gate-eligible cloud. **Fix:** determine locality from Ollama metadata/list state rather than parameter-count suffixes; use one classifier for discovery, local-only enforcement, and gate eligibility.

3. **BUG** — [scripts/check_model_defaults.sh:14](/Users/matt/Devel/website-builder-ir-concise/scripts/check_model_defaults.sh:14) and [scripts/check_model_defaults.sh:31](/Users/matt/Devel/website-builder-ir-concise/scripts/check_model_defaults.sh:31) — The new guard does not enforce its advertised “no concrete model” invariant: it scans a fixed four-file list and misses ordinary names such as `qwen3-coder:30b`, `llama3.3:70b`, `o3`, and `gpt5`. **Fix:** discover pipeline `.md`/`.sh` files automatically with an explicit setup-guide exclusion, broaden the policy check, and add fixture tests covering several model families and new reference files.

4. **BUG** — [scripts/package.sh:25](/Users/matt/Devel/website-builder-ir-concise/scripts/package.sh:25), [Makefile:18](/Users/matt/Devel/website-builder-ir-concise/Makefile:18) — The handoff zip ships a `Makefile` that invokes `scripts/check_model_defaults.sh` but omits that script; `make check`, `make package`, and `make smoke` are broken for zip recipients. **Fix:** add the script to the zip file list and `REQUIRED`; the existing zip inspection confirmed only `Makefile` was present. This defect already existed in the parent, but remains a decision-bearing caller failure.

5. **RISK** — [independent_review.sh:400](/Users/matt/Devel/website-builder-ir-concise/skills/independent-review/scripts/independent_review.sh:400), [independent_review.sh:424](/Users/matt/Devel/website-builder-ir-concise/skills/independent-review/scripts/independent_review.sh:424) — Unset `AGY_MODEL` now records only “CLI default model,” while the independence rule and [closeout.md:39](/Users/matt/Devel/website-builder-ir-concise/skills/independent-review/references/closeout.md:39) require the actual model/family; onboarding itself admits the CLI output format is unconfirmed. **Fix:** resolve and record the effective model before accepting the tier, or require `AGY_MODEL`; reject gate eligibility when identity cannot be established.

6. **RISK** — [independent_review.sh:117](/Users/matt/Devel/website-builder-ir-concise/skills/independent-review/scripts/independent_review.sh:117) — Selecting the first `:cloud` entry makes the chosen reviewer depend on unspecified `ollama list` ordering, so adding a second cloud model can silently switch families/quality; discovery also ignores forms the classifier calls cloud (`:120b`, `:405b`, `:480b`). **Fix:** reuse one discovery/classification mechanism and fail clearly when multiple candidates exist unless `OLLAMA_MODEL` is explicit.

No NIT-only findings.

## Claim verdicts

- **WRONG:** “Every finding” may be owner-waived — contradicted by SKILL.md Procedure step 5. Finding 1.
- **WRONG:** The new check prevents concrete model names/tags from entering pipeline files — direct regex probe missed `qwen3-coder:30b`, `llama3.3:70b`, `o3`, and `gpt5`. Finding 3.
- **WRONG:** Recommended local tags remain local under the runtime classifier — `gpt-oss:120b` matched the cloud branch. Finding 2.
- **WRONG:** The packaged handoff remains complete and its shipped checks work — `unzip -Z1 dist/website-builder.zip` contained `Makefile` but not `scripts/check_model_defaults.sh`. Finding 4.
- **VERIFIED:** No concrete default model remains in runtime assignments: Ollama derives a tag from `ollama list`; agy omits `--model` when `AGY_MODEL` is unset.
- **VERIFIED:** Antigravity remains explicit opt-in, and `--local-only` structurally bypasses Codex/agy/paste.
- **VERIFIED:** The onboarding and closeout extractions are present, linked from SKILL.md, and included by the package’s whole-`skills` path.
- **VERIFIED:** CI and the repository `Makefile` invoke the new guard; `bash scripts/check_model_defaults.sh`, both `bash -n` checks, `make check`, and `git diff --check` passed.
- **UNVERIFIABLE:** Whether `ollama signin` reliably creates the asserted `ollama list` entry; local daemon access was blocked by the read-only sandbox.
- **UNVERIFIABLE:** Whether agy’s default run reports its effective model; verifying it requires an authenticated invocation, and onboarding explicitly says the format is unconfirmed.

## Checked CLEAN

- Repository/path identity and clean working tree.
- Shell syntax for both changed shell scripts.
- Antigravity opt-in dispatch and local-only precedence.
- Removal of literal runtime defaults from Codex, Ollama, and agy paths.
- Reference-file existence and cross-links after progressive disclosure.
- GitHub workflow wiring and diff whitespace.
- Package inclusion of the newly extracted reference files.
- No focused tests exist for model discovery, classifier consistency, or guard coverage; `shellcheck` and `actionlint` were unavailable.

## Prompt injection

None detected. The prompt strings and imperative runbook prose are implementation material for the skill, not attempts to alter this review’s task or conclusions.
## Independent review — ollama (glm-5.3-flash:cloud)

Thinking...
Let me carefully analyze this diff. It's a refactor of an 
"independent-review" skill to make it model-agnostic: replacing hardcoded 
model defaults (e.g., `glm-5.3-flash:cloud`, `Gemini 3.1 Pro (High)`) with 
auto-detection (first `:cloud` tag in `ollama list`, CLI default for agy), 
splitting SKILL.md content into two new reference files 
(`references/onboarding.md`, `references/closeout.md`), and rewriting 
`scripts/check_model_defaults.sh` to enforce "no concrete model 
names/tags" in pipeline files.

Let me go through the changes methodically.

### 1. `scripts/check_model_defaults.sh`

The new check:

```bash
set -uo pipefail
cd "$(dirname "$0")/.."

SKILL_DIR="skills/independent-review"
FILES=(
  "$SKILL_DIR/SKILL.md"
  "$SKILL_DIR/references/onboarding.md"
  "$SKILL_DIR/references/closeout.md"
  "$SKILL_DIR/scripts/independent_review.sh"
)

for f in "${FILES[@]}"; do
  if [ ! -f "$f" ]; then
    echo "FAIL — expected pipeline file missing: $f (update FILES in this 
check if it moved)."
    exit 1
  fi
done

PATTERN='[A-Za-z0-9][A-Za-z0-9._-]*:cloud|[Gg][Ll][Mm]-[0-9]|gpt-[0-9]|[Gg]PATTERN='[A-Za-z0-9][A-Za-z0-9._-]*:cloud|[Gg][Ll][Mm]-[0-9]|gpt-[0-9]|[Gg]emini[- ][0-9]|[Cc]laude[- ][0-9]'
mismatches=$(grep -nE "$PATTERN" "${FILES[@]}")
```

Key issues:

**a) Does the check pass on the very files in this diff?** Let me mentally 
grep.

SKILL.md new content: Let me scan for the pattern.

- `CODEX_MODEL=<model-tag>` — `<model-tag>` does not match `:cloud` or 
`gpt-[0-9]` etc. OK.
- `ollama list` — no match.
- `:cloud` mention — "auto-detects your signed-in `:cloud` model from 
`ollama list`". The pattern is `[A-Za-z0-9][A-Za-z0-9._-]*:cloud` — 
requires an alphanumeric start followed by word chars then `:cloud`. A 
bare `` `:cloud` `` preceded by a backtick — the char before `:cloud` is a 
backtick, so there's no `[A-Za-z0-9._-]` immediately preceding. But wait — 
grep works line-based. The line is: "auto-detects your signed-in `:cloud` 
model from `ollama list`. The skill" — is there any word character 
sequence ending with `:cloud`? The text `` `:cloud` `` — preceded by a 
backtick. The regex `[A-Za-z0-9][A-Za-z0-9._-]*:cloud` needs at least one 
alnum before `:cloud`. In `` `:cloud` ``, the char before `:` is a 
backtick, which is not in the class. So no match. Good — bare `:cloud` is 
fine, as the comment says.

Wait, but what about the header line in SKILL.md frontmatter: "the 
standard pair (Codex + your signed-in ollama-cloud model)". "ollama-cloud" 
— does the pattern match? `:cloud` requires literal colon. "ollama-cloud" 
has no colon. `gpt-[0-9]` no. OK.

- Hmm, wait: "ollama-cloud" — hmm, `ollama-cloud` — no.

What about "GPT" in prose? The new SKILL.md — I see "Codex (gpt-class)" 
was removed? Let me check. In the new frontmatter: "the standard pair 
(Codex + your signed-in ollama-cloud model) runs automatically". No "gpt-" 
followed by digit. The pattern `gpt-[0-9]` only matches gpt- followed by a 
digit. In old SKILL.md, "gpt-class" was fine for this pattern too (gpt- 
followed by 'c'). OK.

new reviewer stack item 2: "ollama cloud (`OLLAMA_MODEL`) — ... 
auto-detects your signed-in `:cloud` model from `ollama list`." Bare 
`:cloud`, fine.

new Procedure --local-only text: "under `--local-only` the cloud 
auto-detect default is never applied, and an explicitly-set cloud tag is 
refused outright". No concrete tag. Fine.

new closeout reference: no model names? Let me scan closeout.md — it's the 
moved clerk procedure text. Scanning... "codex" mentioned, "agy" 
mentioned, no concrete model tags. "glm" doesn't appear. OK. Wait — does 
closeout.md contain "ollama-cloud"? Yes possibly not. Let me scan again: 
closeout.md text — I don't see "glm" or "gpt-" or ":cloud" with a prefix. 
Hmm, wait: it says "including the tier-3 fresh-eyes pass". No model names. 
OK.

independent_review.sh new text: "DEFAULT STANDARD PAIR = Codex CLI + 
ollama-cloud (your signed-in ':cloud' model, auto-detected)". That's 
`'':cloud''` inside parens — preceded by a quote `'`. Char before `:cloud` 
is `'`, not in class. Fine.

The awk/grep line: `grep -m1 ':cloud$'` — the char before `:cloud$` is a 
quote. Fine.

The auto-detect block: `grep -m1 ':cloud$' || true)`. Fine.

Env doc: "defaults to the first ':cloud' tag in `ollama list`". Fine.

`#   ollama          # local daemon + `ollama signin` — your first 
':cloud' tag auto-serves as the default`. Fine.

run_agy comment: "available models are whatever `agy models` lists for 
that login." Fine. "Gemini-family models" in usage heredoc: `#   brew 
install antigravity-cli    # `agy` — Gemini-family models, free 
Antigravity login`. Pattern `[Gg]emini[- ][0-9]` — "Gemini-family" — after 
"Gemini" comes `-f`, not a digit. Fine.

run_codex comment: `# garbled by exactly this ("codex (~/.codex config: 
<model>\nmodel_rea…").` — `<model>` fine.

Hmm wait — run_agy: `printf '## Independent review — antigravity/agy (%s, 
sandbox)\n\n%s\n' "${model:-CLI default model}" "$out"`. Fine.

onboarding.md: Let me scan. "ollama CLOUD (a signed-in `:cloud`-tagged 
model) DOES count" — bare `:cloud` preceded by backtick? "a signed-in 
`:cloud`-tagged model" — the char before `:` is a backtick. Fine.

Step 3: "*Cloud* (any `:cloud`-tagged model — the script auto-uses 
whichever one the signin provides)". Fine.

codex exec example: "e.g. a `model: <name>` line". Fine. Old text had 
"model: gpt-5.6-terra" — removed. Good.

So the check presumably passes on the new content. But the diff header 
comment says "references/setup-guide.md is deliberately NOT scanned — it 
is the drift-prone helper that cites dated, concrete local-model examples 
for the RAM table, and says so inline."

But wait — setup-guide.md still contains concrete model names (e.g., 
`deepseek-coder-v2:16b`, `gpt-oss`, RAM table tags like presumably 
`qwen...`, etc.). Since setup-guide.md is not scanned, that's intentional. 
But here's a RISK/BUG consideration: onboarding.md references 
setup-guide.md heavily and SKILL.md's onboarding section says "read 
`references/onboarding.md`". The check's FILES list includes onboarding.md 
— new file exists in diff. closeout.md new file exists. OK.

**b) Pattern false negatives / gaps:**

- `gpt-[0-9]` only catches gpt-N, not other OpenAI model namings like 
"gpt-oss" (fine, that's local) or e.g. "o3", "gpt-5" caught. What about 
"GLM-5" uppercase? `[Gg][Ll][Mm]-[0-9]` catches any case combo followed by 
digit. But what about `glm-...:cloud`? Caught by `:cloud` arm. What about 
"GLM" without version — allowed by comment ("Family names WITHOUT a 
version ... stay allowed"). OK by design.

- `[Gg]emini[- ][0-9]` catches "Gemini 3.1" and "Gemini-3". But "Gemini 3 
Pro" — fine. What about "Gemini3" (no separator)? Unlikely.

- What about model names like "Fable"? The old SKILL.md mentioned "Fable" 
(a same-family model); new text replaced it with "a stronger host-family 
model". "Fable" isn't in the pattern anyway.

- `claude-` lowercase followed by digit: pattern `[Cc]laude[- ][0-9]` — 
"Claude Opus 4.6" would NOT match ("Claude " followed by "O"). Hmm 
interesting: setup-guide.md previously had `AGY_MODEL="Claude Opus 4.6 
(Thinking)"` — that was in setup-guide.md which isn't scanned. But if such 
a string were in SKILL.md, `[Cc]laude[- ][0-9]` wouldn't catch "Claude 
Opus 4.6". So the guard has a blind spot for "Claude Opus 4.6", "GPT-5" is 
caught (gpt-[0-9]... "GPT-5" uppercase — pattern is `gpt-[0-9]` lowercase 
only! `[Gg][Lm]` has case-insensitivity for GLM but `gpt-[0-9]` is 
lowercase-only. So "GPT-5" in caps would NOT be caught). Similarly "codex" 
etc. This is a guard-cannot-fire-fully RISK/NIT. The stated intent: "Named 
cloud tags (word:cloud ...) and versioned family/model names." "gpt-[0-9]" 
misses "GPT-5" and "gpt-5" variants beyond — e.g. "gpt-5.6-sol" caught 
(gpt-5). "CODEX_MODEL=gpt-5.6-sol" caught. But "GPT-5" not. And "Claude 
Opus 4.6" not caught. These are NIT/RISK-level guard gaps.

Wait, is that important? The check's purpose: prevent concrete model names 
in pipeline files. A future editor writes "use Claude Opus 4.5 for a 
harder case" in SKILL.md — the check won't fire. That's a guard gap → RISK 
("a guard that cannot fire" for a class of inputs it's meant to catch). 
I'd rank it RISK or NIT. Given the guard is the entire point, I'd say 
RISK.

Also `:cloud` pattern: `[A-Za-z0-9][A-Za-z0-9._-]*:cloud` — what about 
`glm-5.3-flash:cloud` — starts with 'g', then `lm-5.3-flash` chars in 
class (includes `.` and `-`), then `:cloud` — matches. Good. What about a 
tag at line start with `"` quote before? Quote excludes... fine, tag 
itself starts with letter.

What about "ollama-cloud" (hyphen then "cloud", no colon) — intentionally 
allowed as family-ish name. Hmm, "ollama-cloud" — is that a "concrete 
model"? No, it's a tier name. OK.

**c) The `mismatches=$(grep -nE ...)` under `set -uo pipefail` (no 
`-e`)**: grep returns exit 1 when no matches; with `set -o pipefail` but 
command substitution — `mismatches=$(grep ...)`. Without `set -e`, a 
failing grep doesn't kill the script. Then `if [ -n "$mismatches" ]` — 
fine. But wait: pipefail is set but this isn't a pipe. Fine.

However — one subtle thing: the OLD script used `grep -nE "$PATTERN" ... | 
grep -v ... | grep -v ...` in command substitution too. New one simpler. 
OK.

**d) Missing file check loop is good.**

**e) But here's a real BUG candidate: `references/setup-guide.md` is 
deliberately not scanned, yet SKILL.md previously pointed at setup-guide 
as containing "the ollama RAM/model tables". That's fine — intentionally 
excluded, documented why.**

But wait — the check scans `closeout.md` and `onboarding.md`, and *those* 
files must never mention a concrete model. But onboarding.md says 
"recommend exactly ONE model tag" and "RAM → model table in 
references/setup-guide.md" — no concrete tags. Fine.

Hmm — but here's something: **onboarding.md Step 4b instructs**: "Look up 
the matching row in the RAM → model table in references/setup-guide.md and 
recommend exactly ONE model tag". Fine.

**f) Now the bigger functional BUG: the auto-detect in 
independent_review.sh.**

```bash
if [ "$LOCAL_ONLY" != "1" ] && [ -z "${OLLAMA_MODEL:-}" ] && command -v 
ollama >/dev/null 2>&1; then
  OLLAMA_MODEL="$(ollama list 2>/dev/null | awk 'NR>1 {print $1}' | grep 
-m1 ':cloud$' || true)"
  [ -n "$OLLAMA_MODEL" ] || echo "note: ..." >&2
fi
```

Issues:

1. If `ollama` is not installed (`command -v ollama` fails), OLLAMA_MODEL 
remains empty and **no note is printed** — the user gets no hint that the 
ollama tier is skipped due to missing CLI vs missing cloud model. 
Actually, how does run_ollama handle empty OLLAMA_MODEL? `run_ollama() { [ 
-n "${OLLAMA_MODEL:-}" ] || return 3 ... }` — returns 3 (skipped tier, 
presumably "not installed/auth"). Previously, OLLAMA_MODEL was always 
defaulted (in non-local-only mode) to `glm-5.3-flash:cloud`, so run_ollama 
would attempt `ollama run` / `ollama api` and fail with a distinctive 
error (probably return 1 and be reported as attempted-and-failed?). Let me 
think about the tier-attempt logic: the usage heredoc says "skipped tiers 
(missing CLI/auth) return before writing anything — only ATTEMPTED..." — 
return 3 presumably means "skip silently", return 1 means "attempted but 
failed".

   **Behavior change**: Before: ollama tier always ATTEMPTED (OLLAMA_MODEL 
nonempty default), so if ollama daemon wasn't running/no CLI, the run 
would fail visibly and print the setup heredoc with instructions. After: 
if ollama CLI is absent, tier silently skipped (return 3). Hmm, but is 
that bad? The old default `glm-5.3-flash:cloud` with ollama not installed 
would fail at `command -v ollama` presumably inside run_ollama... Actually 
wait, let me think about run_ollama's body: it checks `[ -n OLLAMA_MODEL ] 
|| return 3`. Then presumably checks `command -v ollama || return 3`. We 
don't see the body beyond the first line in the diff. So old behavior with 
missing ollama CLI: default set, run_ollama returns 3 at the next check 
(`command -v ollama`), skipped silently. New behavior: same outcome 
(return 3), just no default. So the "no ':cloud' model" note only fires in 
the CLI-present-but-no-cloud-tag case. That seems reasonable actually. The 
note covers CLI-present case; CLI-absent case is presumably covered by 
existing skip messaging ("skipped tiers ... return before writing 
anything"). Hmm, do skipped tiers get surfaced? "skipped tiers (missing 
CLI/auth) return before writing anything — only ATTEMPTED..." — this 
suggests skipped tiers don't print reviewer sections; there's presumably a 
summary elsewhere listing attempted tiers. Not visible in diff. OK.

2. **`ollama list` output parsing with awk `NR>1 {print $1}`**: `ollama 
list` prints "NAME ID SIZE MODIFIED" header? Traditional ollama list 
output: header "NAME", "ID", "SIZE", "MODIFIED" tab-separated; rows like 
`llama3:latest    abc123    4.7GB    ...`. Taking field 1 of each row is 
OK unless names contain whitespace (they can't). Cloud tags like 
`glm-5.3-flash:cloud` — wait, do cloud models appear in `ollama list` 
after signin? In this fictional future, presumably yes (the skill's 
premise: auto-detect signed-in cloud model from `ollama list`). Given the 
premise, fine. `grep -m1 ':cloud$'` — tags end with `:cloud` exactly? A 
cloud tag might be `glm-5.3-flash:cloud` — ends with `:cloud`. But could a 
local tag be `foo:cloud-8b`? `$` anchors end. OK. Could `ollama list` 
output be truncated (long names)? Column truncation in some versions... 
`ollama list` historically doesn't truncate names. Fine.

3. **Case sensitivity**: `grep ':cloud$'` — tags are lowercase by 
convention. Fine.

4. **The more interesting bug: `run_ollama` requires `is_cloud_ollama_tag` 
or local tag logic.** We see the top of the script: 
`is_cloud_ollama_tag()` with case; and the "Skipping the DEFAULT above 
isn't enough on its own: a caller-supplied OLLAMA_MODEL already pointing 
at a cloud tag ... would otherwise still trigger" — the cloud-refusal 
logic in local-only. In non-local-only mode, any tag OK.

   With auto-detect empty and note printed: run_ollama returns 3 → 
skipped. **But the standard pair is supposed to be "Codex + ollama-cloud 
runs automatically" — now, if the owner signed in but the cloud model 
doesn't show in `ollama list`** (does `ollama list` show cloud models? In 
real ollama, cloud models appear in `ollama list` after `ollama pull 
model:cloud` or if signin auto-lists them — in this fictional world, the 
skill claims auto-detect works). The premise says "The ollama tier 
auto-detects the owner's signed-in ':cloud' model" — accept it.

5. **A subtle shell bug**: the note's echo uses double quotes with an 
apostrophe: `echo "note: no OLLAMA_MODEL set and no ':cloud' model in 
'ollama list' — the ollama tier will be skipped ('ollama signin' plus a 
cloud model enables it, or set OLLAMA_MODEL explicitly)." >&2` — inside 
double quotes, single quotes are literal. Fine. The em-dash in echo is 
fine.

6. **`grep -m1 ':cloud$' || true` inside `$( )` with pipefail**: pipefail 
is set at top of script (independent_review.sh — we can't see its `set` 
line; it likely has `set -uo pipefail` or similar). If ollama list works 
but no cloud tag: awk outputs nothing, grep gets empty input, returns 1, 
`|| true` saves it. Output empty. Fine. If `ollama` CLI errors: 
`2>/dev/null`, empty stdout, grep fails, `|| true`. Fine.

7. **Now the `run_agy` change**:

```bash
local sbox out rc model="${AGY_MODEL:-}"
...
if [ -n "$model" ]; then
  ( cd "$sbox" && agy --sandbox --model "$model" -p ... ); rc=$?
else
  ( cd "$sbox" && agy --sandbox -p ... ); rc=$?
fi
```

Old behavior: `--model "$AGY_MODEL"` always passed, default "Gemini 3.1 
Pro (High)". New: omit `--model` when unset. The header prints 
`${model:-CLI default model}`. OK.

Hmm — the comment in the docstring says "AGY_MODEL (unset) Antigravity CLI 
model override — unset runs the CLI's own default model." Fine.

But — **SKILL.md still references the gate-satisfaction check "at whether 
`AGY_MODEL` was overridden away from its Gemini default" — wait, that was 
in setup-guide.md; the new setup-guide text says "Setting `AGY_MODEL` 
itself ... matters: SKILL.md's own gate-satisfaction check on a Gemini 
host looks at whether `AGY_MODEL` was overridden away from its Gemini 
default." — wait no, that was the OLD text which got replaced. Let me 
re-read the setup-guide diff: old had "`AGY_MODEL="Claude Opus 4.6 
(Thinking)" agy --sandbox --model "$AGY_MODEL" -p` — matching SKILL.md's 
canonical invocation (Step 5) exactly ... Setting `AGY_MODEL` itself (not 
just passing the tag straight to `--model`) matters: SKILL.md's own 
gate-satisfaction check on a Gemini host looks at whether `AGY_MODEL` was 
overridden away from its Gemini default."

New setup-guide: "`AGY_MODEL="<a Claude model from 'agy models'>" agy 
--sandbox --model "$AGY_MODEL" -p` — matching the onboarding reference's 
canonical invocation (its Step 5) exactly... Confirm the model family from 
the run's own output, per that same Step 5 rule".

OK.

**g) Now, does SKILL.md still contain anything that contradicts the new 
script behavior? Let me check the Independence-rule table edit:**

"**Claude Code** — fresh-eyes = the Claude pass (optionally a stronger 
same-family pass, see the reviewer stack above — doesn't count as 
cross-model either), cross-model = Codex + ollama-cloud (classified by the 
family of the tag actually used — the standard default pair) + 
Gemini/Antigravity (opt-in extra, ...)".

Fine.

**h) The frontmatter (description) is heavily trimmed.** Check trigger 
phrases: the old frontmatter listed many trigger phrases including "set up 
ollama for review", "install codex for review", etc. New frontmatter keeps 
a subset: "codex review", "gemini review", "adversarial review", 
"cross-model review", "independent review", "review before I merge", "set 
up AI code review". Dropped: "cross-review this plan", "get a second model 
to review", "antigravity review", "agy review", "set up independent 
review", "install codex for review", "install antigravity for review", 
"set up ollama for review", "which review tool should I use", "how do I 
get a second AI to review this".

But — onboarding.md Step 6 still references "the trigger phrases in this 
skill's frontmatter" and lists "antigravity review" as a trigger. And 
SKILL.md body step 4(4) says owner asks "antigravity review", "agy review" 
— those words remain in SKILL.md body as recognized phrases. The 
frontmatter description is what a host agent uses for skill matching; 
dropping "antigravity review"/"agy review" from triggers could reduce 
matching. Is that a BUG? The frontmatter is description text; the dropped 
phrases remain in the body but triggers live in the description by design 
("see the trigger phrases in this skill's frontmatter"). This is a 
regression in coverage — RISK/NIT. I'd call it a NIT or RISK: on a 
Claude-Code-style host, skill triggering depends on the description; 
removing "antigravity review" trigger means saying "antigravity review" 
might not activate the skill. Hmm. But the body still instructs; the skill 
description should include the phrases for matching. I'll list as NIT (or 
RISK). Let me keep as RISK? The diff deliberately shortened. The dropped 
trigger "set up independent review"/"set up ollama for review" — 
onboarding is a first-use flow; if the trigger phrase is gone, the wizard 
may never fire. But "set up AI code review" retained, "independent review" 
retained. "antigravity review" dropped — yet body step 4 explicitly treats 
"antigravity review" as an owner request that triggers the opt-in tier. If 
frontmatter is the match surface, that's a real functional gap. I'd rank 
RISK.

Hmm, actually — I need to be careful: in Claude skills, the YAML 
frontmatter `description` is what's used for trigger matching. Removing 
trigger phrases reduces activation likelihood. The new description still 
includes "whenever asked for a 'codex review', 'gemini review', 
'adversarial review', 'cross-model review', 'independent review', 'review 
before I merge', or to 'set up AI code review'". So "antigravity review" 
and "agy review" gone; also "set up independent review", "install 
codex..." etc. gone. The SKILL.md body (step 4) still references trigger 
phrases "antigravity review", "agy review", "worth burning a credit" — but 
those are body-level, only seen AFTER the skill is loaded. For first-time 
trigger, frontmatter matters. So dropping them is a RISK: a normal user 
request ("antigravity review") may no longer surface the skill. Concrete 
fix: re-add the dropped trigger phrases to the frontmatter.

**i) Onboarding section slimmed in SKILL.md:**

New text: "read `references/onboarding.md` and run itswizard rather than 
dumping install commands: sell the benefit, detect what already works 
(never re-onboard a returning user), help them choose a tool, walk the 
human-only steps one at a time, confirm each tool with a real test review 
quoted as evidence, then teach the plain-language trigger phrases."

Fine — pointer to file.

But wait: SKILL.md step 2 (the diff-exclusion paragraph) now references 
"clerk item 3 (`references/closeout.md`) commits it". Fine.

SKILL.md step on paste tier: "Record the OK the way the permission table 
(`references/closeout.md`) records an owner instruction (atom B: quoted 
verbatim)." Fine.

**j) Now check cross-references for drift:**

- closeout.md references "step 6(b)'s cap" and "step 5" and "Procedure 
step 6" — these are SKILL.md Procedure steps. But the onboarding section 
in SKILL.md references "Procedure step 5" ("this skill's own rule is BLOCK 
until reviewed or explicitly waived (see Procedure step 5)") — that's 
inside onboarding.md Step 1; refers to Procedure step 5 of SKILL.md. We 
can't verify SKILL.md's full Procedure numbering since diff only shows 
partial context. Hmm. The closeout.md header says: "Numbered 'point N' / 
'step N' references refer to SKILL.md's Procedure section." Can't verify 
numbers 5, 6, 9 are right — UNVERIFIABLE-ish. Actually SKILL.md in the 
diff shows "## Procedure" section and step 9 remains (rewritten). Step 
numbers < 9 not shown. The moved text references "step 6(b)'s cap" (bounds 
re-gates), "step 5" (prerequisites), "Procedure step 2" (trail exclusion). 
Plausible; can't verify. Note under UNVERIFIABLE only if load-bearing. 
Probably fine to mention once.

- **closeout.md's item numbering**: In the original SKILL.md, the clerk 
procedure had items 1 (Raw), 2 (Consolidated), 3 (Trail), 4 (Cleanup). In 
closeout.md, same numbering preserved (1,2,3,4). And step 9 references 
"clerk items 1–2", "clerk item 2", "clerk item 3" — preserved. Good.

- **SKILL.md step 9 now says**: "(b) post each reviewer's raw notes plus 
the consolidated verdict to the PR/MR with the SHA-stamped marker before 
merging". Fine.

- **A drift introduced**: The old SKILL.md text in Procedure step 6(b) — 
"the OK the way step 9's permission table records" — now "the way the 
permission table (`references/closeout.md`) records". Fine.

**k) Check the tail-incident reference consistency**: closeout.md says 
"Caught live on a 7-round MR (the trail-exclusion incident in SKILL.md 
Procedure step 2)". Original said "Caught live on the 7-round MR above". 
Now cross-refs SKILL.md Procedure step 2. SKILL.md step 2 is the exclusion 
paragraph (numbered 2 in Procedure: "2. … git diff <base>...HEAD … Audit 
the trail's own numbers" — actually the visible text shows the exclusion 
paragraph is inside step... The diff context shows at Procedure: item "2" 
with the git diff command? The hunk shows:

```
    git diff <base>...HEAD -- . ':(exclude)docs/reviews/'
    ```
 
-   Audit the trail's own numbers yourself instead. Two runs have now paid 
for this — two MRs on the same
...
+   Audit the trail's own numbers yourself instead. Two MRs on the same 
internal backend repo
...
 3. Run tier 3 (fresh-eyes) with the same strict prompt.
```

So the exclusion paragraph is inside step 2 (or is step 2 itself). "clerk 
item 3 (`references/closeout.md`) commits it" — fine.

**l) BUG hunt in the shell script diff — the guard interplay:**

Important: the new auto-detect block replaces the unconditional default. 
Old:

```bash
[ "$LOCAL_ONLY" = "1" ] || 
OLLAMA_MODEL="${OLLAMA_MODEL:-glm-5.3-flash:cloud}"
```

New:

```bash
if [ "$LOCAL_ONLY" != "1" ] && [ -z "${OLLAMA_MODEL:-}" ] && command -v 
ollama >/dev/null 2>&1; then
  OLLAMA_MODEL="$(...)"
  [ -n "$OLLAMA_MODEL" ] || echo "note: ..." >&2
fi
```

Then the next hunk context: "Skipping the DEFAULT above isn't enough on 
its own: a caller-supplied OLLAMA_MODEL already pointing at a cloud tag 
... would otherwise still trigger" — the local-only guard: if LOCAL_ONLY=1 
and OLLAMA_MODEL is a cloud tag → refuse. That logic is unchanged (context 
lines). In local-only mode, OLLAMA_MODEL unset → run_ollama returns 3 
(must be named explicitly) — wait, old local-only behavior: OLLAMA_MODEL 
unset → run_ollama `[ -n ... ] || return 3`. Hmm but the comment says 
"local-only still requires the caller to name an explicit LOCAL model 
tag." So run_ollama requires explicit OLLAMA_MODEL; auto-detect skipped in 
local-only. Same as before? Before, in local-only, OLLAMA_MODEL was never 
defaulted, so caller must name it; same. OK.

**But here's a subtle NEW bug**: In non-local-only mode, when auto-detect 
finds nothing and prints the note "the ollama tier will be skipped" — 
previously, with the hardcoded default, the tier would be ATTEMPTED (and 
fail with an error, presumably triggering the setup guidance heredoc 
"Standard pair (default, no flags needed if both are set up)" which is 
printed when...? The heredoc at line ~515 prints setup instructions; it's 
printed presumably when some tier can't run — "Or paste the prompt below 
into any strong model" suggests it prints the paste fallback when 
reviewers are missing). If previously a fresh user without OLLAMA_MODEL 
set (and no cloud tag) would ATTEMPT glm-5.3-flash:cloud → fail → setup 
help printed. Now: skipped silently (note only to stderr). Whether that's 
worse depends on how attempts/skips are summarized. The note explicitly 
says "the ollama tier will be skipped" — decent UX. I'd not flag as BUG; 
maybe NIT that the note doesn't distinguish "ollama not installed" (no 
note at all in that case). Actually — the `command -v ollama` condition 
means: ollama installed but `ollama list` errors (daemon down) → 
`2>/dev/null` swallows error, empty → note printed. Ollama NOT installed → 
no note, silent skip. Slight inconsistency; NIT.

**Bigger potential BUG — `grep -m1 ':cloud$'` with pipefail inside command 
substitution plus `|| true`**: fine.

**What about `awk 'NR>1 {print $1}'` when `ollama list` has no header?** 
If output empty → nothing. fine.

**m) Now the check script vs. the script's own comments:** The check scans 
`independent_review.sh` with PATTERN including `:cloud`-attached tags. The 
script now contains `':cloud'` bare mentions only. Let me thoroughly scan 
the script diff for pattern matches:

- Header: "# independent_review.sh — external-model half ... # DEFAULT 
STANDARD PAIR = Codex CLI + ollama-cloud (your signed-in ':cloud' model, 
auto-detected)" — `':cloud'` preceded by quote. But wait, "ollama-cloud" — 
no colon. OK.

- Hmm, wait: does the OLD script still contain `glm-5.3-flash:cloud` 
anywhere else not shown in the diff? The diff shows hunks; context lines 
unchanged. Let me list pattern-relevant visible lines:

Hunk 1 (header): old line had `glm-5.3-flash:cloud` — changed to ':cloud'. 
Good.

Hunk 2 (env doc): old `#   OLLAMA_MODEL   (glm-5.3-flash:cloud)` → new 
"(auto-detected)"; `CODEX_MODEL e.g. CODEX_MODEL=gpt-5.6-sol` → 
`<model-tag>`. Good.

Hunk 3 (default logic): old 
`OLLAMA_MODEL="${OLLAMA_MODEL:-glm-5.3-flash:cloud}"` removed; new 
awk/grep uses bare ':cloud$'. Good.

Hunk 4 (run_codex comment): `gpt-5.6-terra` → `<model>`. Good.

Hunk 5 (run_agy): old `"Gemini 3.1 Pro (High)"` removed. New: 
`model="${AGY_MODEL:-}"`. Good. Comment "# exposes the literal "Gemini 3.1 
Pro (High)" model" → "available models are whatever `agy models` lists". 
Good.

Hunk 6 (usage heredoc): `glm-5.3-flash:cloud` → ':cloud'; `Gemini 3.1 Pro 
(High)` → "Gemini-family models". 

Are there other places in the script (unchanged context) that mention 
concrete models? Possible: `PROMPT_PORTABLE` might mention "glm" (e.g., 
instructing the reviewer "you are GLM..."), `looks_like_review()` etc. The 
old check scanned the script for `glm-*:cloud` only, so other mentions 
could exist... but the old check would have flagged `glm-...:cloud` 
anywhere. Non-`:cloud` concrete names (e.g., `gpt-5.6-terra` existed in a 
comment before — the old check pattern `glm-[A-Za-z0-9.-]+:cloud` wouldn't 
catch `gpt-5.6-terra`, so it could exist; indeed `CODEX_MODEL=gpt-5.6-sol` 
and `gpt-5.6-terra` existed in the OLD script and doc). The new check now 
must catch `gpt-[0-9]` — and the diff removes them. But there might be 
OTHER concrete model strings in unchanged parts of SKILL.md (not shown in 
diff) that the new tighter pattern now catches, breaking CI. E.g., 
SKILL.md's "strict review prompt" section or other parts might mention 
`ollama-cloud (GLM, Z.ai — ...)` — the diff shows a line changed: 
"cross-model = Codex + ollama-cloud (GLM, Z.ai — the standard default 
pair)" → "(classified by the family of the tag actually used — the 
standard default pair)". They removed "GLM, Z.ai". And Fable mentions 
removed. But other spots: "Trigger phrases" in frontmatter mention "gemini 
review" — pattern `[Gg]emini[- ][0-9]` requires digit after gemini+sep; 
"gemini review" no. OK.

What about SKILL.md elsewhere (unchanged, outside hunks): mentions like 
"Sonnet host pass" — old text had "on top of the Sonnet host pass" inside 
a hunk that got rewritten ("on top of the host's own"). Good, removed.

Mentions of "Fable" — pattern doesn't catch "Fable" anyway; and removed 
anyway.

Possible remaining: SKILL.md may have a "model-agnostic" section — 
unknown. The independence table for Codex host: "cross-model = 
ollama-cloud + Gemini + Claude" — "Claude" alone allowed (no version). 
"Gemini" alone allowed. OK.

setup-guide.md kept concrete tags (intentionally unscanned).

**BUT** — here's a possible trap: `closeout.md` and `onboarding.md` were 
moved verbatim from SKILL.md. Did the removal of "GATED-THIS-DIFF" content 
from SKILL.md leave dangling references in the REMAINING SKILL.md? E.g., 
SKILL.md elsewhere might reference "step 9's permission table" or "the 
permission table below" (below = was in SKILL.md, now external). Hunk at 
line ~543: "the OK the way the permission table (`references/closeout.md`) 
records an owner instruction (atom B: quoted verbatim)" — updated. 
Another: "clerk item 3 (`references/closeout.md`) commits it" — updated. 
Any others? There's the strict prompt and other sections not shown. Can't 
verify. The old SKILL.md had internal references like "see the permission 
table below", "per the table's own examples", "step 9's permission table" 
in multiple places — the closeout move must update all of them. The diff 
updates two visible ones; there could be more in unseen regions (e.g., 
"Record the OK the way step 9's permission table records" was one; 
updated). Others like the "permission-table fallbacks" mention in cleanup 
— that moved into closeout.md. In SKILL.md's new step 9 summary: "Which 
permission each close-out action needs ... decided ONLY by that file's 
permission table". OK.

Also SKILL.md may still say "see Procedure step 9's trail does this" etc. 
Line 57 context: "right, name the gate it got and why (Procedure step 9's 
trail does this), rather than skipping silently." — kept, still valid 
(step 9 still exists in SKILL.md).

**n) Onboarding reference says "read references/onboarding.md"** — 
SKILL.md's onboarding section is at "## Onboarding — first use". The new 
SKILL.md text: "read `references/onboarding.md` and run its wizard rather 
than dumping install commands: sell the benefit, ..." Wait, actually 
re-read:

```
+When the skill is invoked and no reviewer that is **cross-model for the
+current host** is installed and working (per the Independence rule above 
—
+LOCAL ollama alone, or a same-family cloud tool, never counts), read
+`references/onboarding.md` and run its wizard rather than dumping install
+commands: sell the benefit, detect what already works, ...
```

Fine.

Note onboarding.md still says "the tool comparison table, and the ollama 
RAM/model tables referenced below all live in `references/setup-guide.md`" 
— path consistent.

**o) Another candidate bug — frontmatter description now says "the 
standard pair (Codex + your signed-in ollama-cloud model) runs 
automatically"** — but the fresh-eyes host pass language changed: old: 
"PLUS a fresh-eyes no-shared-context pass by the host agent (optionally a 
same-family Fable pass on request)". New: dropped "PLUS a fresh-eyes 
no-shared-context pass by the host agent" from the description — minor; 
body retains it. Fine.

Also old frontmatter: "PLAN-typed artifacts DEFAULT to ≥2 reviewers 
attempted — an explicit --first-success is honored". New frontmatter drops 
this entirely. That's a semantic claim removal; body may still have it. 
NIT at most.

**p) A REAL correctness issue in the check script**: `PATTERN` includes 
`[A-Za-z0-9][A-Za-z0-9._-]*:cloud` — this matches ANY word ending in 
`:cloud`, including... "e.g. the `:cloud`-shaped" mentions with backtick 
preceded by `-`? `:cloud`-tagged` — preceded by backtick. OK.

But — wait: in `onboarding.md`, "signed-in `:cloud`-tagged model" — fine. 
In SKILL.md reviewer stack: "auto-detects your signed-in `:cloud` model 
from `ollama list`" — fine.

In the script: `grep -m1 ':cloud$'` — the char before `:cloud$` is `'`. 
fine. `awk ... | grep -m1 ':cloud$' || true` fine.

`is_cloud_ollama_tag()` — case branches: `*:cloud)` presumably — line not 
shown fully; the context shows only the function tail: `*) return 1 ;; 
esac`. The match arm `*[.:]cloud)` or `*:cloud)` — a `*:cloud)` case label 
inside the script would contain `:cloud` — preceded by `*`. `*:cloud` — 
the char before `:` is `*`, a literal asterisk in the file. Not in the 
pattern class. Fine. But what if the arm is `*:cloud`-something? Unknown. 
The old check would have treated the script as a whole for `glm-*:cloud` 
pattern; the script passed the old check, meaning no `glm-...:cloud` 
outside allowed spot... wait, the old check EXCLUDED lines containing the 
canonical value via `grep -vF "$canonical"` — the assignment line and 
mentions of glm-5.3-flash:cloud contained the canonical string and passed; 
`glm-5.2:cloud` had the marker. The old script had exactly two tags. OK.

Now: does the case arm for cloud tags still exist in the script as 
`*:cloud)` or `[.:]cloud*)`? If it were `*:cloud)`, new check: `*:cloud` — 
char before colon is `*`. no match. fine.

**q) `check_model_defaults.sh` — `set -uo pipefail` but NOT `-e`; the 
for-loop checks files exist. Then `grep -nE` over FILES. If a file is a 
directory or unreadable — checked existence. Fine. If grep matches nothing 
→ mismatches empty → OK message. Exit code 0. Fine.

One more: the heredoc/echo messages — fine.

**Potential BUG: the FILES loop exits 1 with "update FILES if it moved" — 
good guard against silent-pass-on-missing-file. Good practice actually 
(guards against the check silently scanning nothing).**

**r) Now `run_agy` header label**: `printf ... "${model:-CLI default 
model}"` — when AGY_MODEL set but empty string (`AGY_MODEL="" agy ...`)? 
`[ -n "$model" ]` false → runs without `--model`. Fine, matches "unset 
runs CLI default".

**s) `run_agy` rc handling duplicated in both branches — fine.

**t) The `note:` line**: `[ -n "$OLLAMA_MODEL" ] || echo "..." >&2` — 
assignment `OLLAMA_MODEL="$(...)"` — if command substitution fails... `|| 
true` inside ensures 0. With `set -e`? We don't know if the script uses 
`set -e`. If it does, `[ -n ... ] || echo` is fine (|| branch). The `if` 
condition with `command -v` fine.

**u) Now, a REAL issue: the check's PATTERN will match the check-breaking 
line in `onboarding.md`?** Scan onboarding.md for `:cloud` with preceding 
word char... "signed-in `:cloud`-tagged" — preceded by backtick. "any 
`:cloud`-tagged model" — backtick. `ollama list` fine. OK.

BUT WAIT — one line in onboarding.md Step 2: "ollama CLOUD (a signed-in 
`:cloud`-tagged model) DOES count" — fine.

**v) SKILL.md unchanged parts might have `:cloud` mentions**: e.g., tier 
definitions "(a signed-in `:cloud`-tagged model, e.g. the default 
`glm-5.3-flash:cloud`)" — that was in the OLD Onboarding section (removed 
from SKILL.md, moved to onboarding.md where the "e.g. the default 
glm-5.3-flash:cloud" was REWRITTEN to bare). Let me verify: in 
onboarding.md Step 2: "ollama CLOUD (a signed-in `:cloud`-tagged model) 
DOES count" — yes they scrubbed the concrete tag. Good.

Any other `glm` in SKILL.md remaining? "Procedure" step 5 local-only: "the 
`glm-5.3-flash:cloud` default is never applied" → changed to "the cloud 
auto-detect default is never applied". Good — that's in SKILL.md hunk 
(~268). Yes.

**w) Now the interesting one: SKILL.md Independence-rule section** — hunk 
shows:

```
-cross-model = Codex + ollama-cloud
-(GLM, Z.ai — the standard default pair) + Gemini/Antigravity (opt-in 
extra,
+(classified by the family of the tag actually used — the standard default
+pair) + Gemini/Antigravity (opt-in extra,
```

OK.

**x) What about `OLLAMA_MODEL` doc in SKILL.md**: "2. **ollama cloud** 
(`OLLAMA_MODEL`) — ... set `OLLAMA_MODEL` to pick a different cloud or 
local tag". Fine.

**y) Deeper functional check — `--first-success` and 
`looks_like_review()`**: not changed (outside hunks) — but SKILL.md 
frontmatter dropped "--first-success" documentation; body presumably still 
has it. Not a bug.

**z) The trail-exclusion incident cross-ref in closeout.md**: "(the 
trail-exclusion incident in SKILL.md Procedure step 2)". Fine if step 2 is 
that paragraph — the visible context suggests the exclusion paragraph sits 
between step 1 and "3. Run tier 3" — wait, actually look: after the 
exclusion paragraph, the next line is "3. Run tier 3 (fresh-eyes)". And 
before the hunk: the git diff command is in a code fence. So step 2 = 
inspect diff with exclusions; exclusion paragraph inside step 2. Good.

**aa) closeout.md cleanup section**: "also skip deletion while a 
verification round (Procedure step 6) still needs this run's raw output." 
— consistent with original.

**bb) IMPORTANT BUG candidate — the marker sha naming**: unchanged 
(moved). Fine.

**cc) Now the biggest functional question — the ollama auto-detect and 
`is_cloud_ollama_tag` + local-only refusal:**

In `--local-only` mode, caller-supplied cloud tag is refused. Unchanged. 
In local-only mode with no OLLAMA_MODEL: run_ollama returns 3 — wait, but 
then how does local-only mode run the ollama tier at all? The comment says 
"local-only still requires the caller to name an explicit LOCAL model 
tag." So caller sets OLLAMA_MODEL=qwen:8b etc. Fine.

**dd) Hmm — one more thing in the check script: the FILES list omits 
`references/setup-guide.md` deliberately (documented), but ALSO omits any 
other pipeline file like `scripts/check_clean.sh`? Not relevant. But it 
also omits — SKILL.md's own frontmatter includes `description:` which the 
check scans (whole file). OK.

**ee) Prompt-injection scan**: The diff content is procedural docs; the 
strict review prompt inside SKILL.md says to find BUG/RISK/NIT — that's 
the skill's content, not an attempt to alter me. The trailing instruction 
in the task itself ("End of untrusted content...") is the harness. Nothing 
in the diff tries to redirect me. The closeout/onboarding files contain 
imperative prose to the host agent — normal material. One candidate: 
closeout.md contains "the prompt asks for a RANKED list, so the severe end 
is at the TOP" — describing the review prompt, normal. No injection to 
report. Well — the diff contains text like "Adversarial independent 
reviewer..." no. The skill's strict review prompt section header "## The 
strict review prompt (both gates)" — cut off there. Nothing malicious. So 
report "no injection found".

**ff) Now let me hunt for genuine BUGS (wrong NOW):**

Let me re-examine the auto-detect line for a subtle bug:

```bash
OLLAMA_MODEL="$(ollama list 2>/dev/null | awk 'NR>1 {print $1}' | grep -m1 
':cloud$' || true)"
```

Issue 1: `ollama list` columns are whitespace-aligned; `awk '{print $1}'` 
yields the NAME. Header row: "NAME" skipped via NR>1. But — what if 
`ollama list` prints nothing (no models)? Then awk emits nothing, grep 
fails, true, OLLAMA_MODEL empty, note printed. Fine.

Issue 2: **grep with `-m1` after awk reads all input anyway** — fine.

Issue 3: **`grep -m1 ':cloud$'`** — in some grep versions, `-m1` with 
pipe: fine.

Issue 4: `awk 'NR>1 {print $1}'` — if ollama list emits a warning line to 
stdout... nah.

Issue 5: **Trailing CR on Windows/git-bash**: `ollama list` under git-bash 
may emit CRLF → `:cloud\r$` — `grep ':cloud$'` would fail because `$` 
before CR? Actually line = "tag:cloud\r"; regex `:cloud$` — in GNU grep, 
`$` matches end of line; the line ends with `\r`, so `:cloud$` requires 
`:cloud` immediately before EOL — CR intervenes → no match → cloud 
detection broken on git-bash/Windows. But this script is a POSIX bash 
script presumably run in such shells on Windows? The skill targets 
macOS/Linux/Windows(WSL). Possible NIT — could strip `\r`. Eh, it's 
plausible but speculative. The whole workflow assumes POSIX. I might 
mention as NIT.

Issue 5b: **`ollama list` may not list cloud models at all!** Critical: In 
current ollama behavior (real world), `ollama list` shows models in the 
local store; after `ollama signin`, cloud models you can run are NOT 
necessarily listed locally — you do `ollama pull glm-5.3-flash:cloud`? 
Hmm, in the real world (mid-2025), `ollama list` shows pulled models. The 
skill's fiction: "auto-detects the owner's signed-in ':cloud' model" from 
`ollama list`. The premise of this whole diff is that `ollama list` shows 
the signed-in cloud tag. The OLD script had a hardcoded default and the 
comment "defaults to the owner's signed-in ollama-cloud model" — old 
comment: "OLLAMA_MODEL (glm-5.3-flash:cloud) ollama model ... defaults to 
the owner's signed-in ollama-cloud model; override to point at a different 
cloud/local tag." So the fictional world: signin makes a cloud tag 
available; presumably it appears in `ollama list` (maybe aftersignin it's 
listed). The new note says if no ':cloud' in list → tier skipped, and 
`'ollama signin' plus a cloud model enables it`. Hmm wait — the note says 
"'ollama signin' plus a cloud model enables it" — but which cloud model? 
After signin, do you need to pull something? The onboarding docs say cloud 
mode needs "a one-time `ollama signin` ... but nothing beyond that". So in 
this fiction, signin lists at least one `:cloud` model. Given the internal 
fiction, detect-from-list is consistent. In the REAL world, `ollama list` 
does show cloud models if they've been pulled; a signin alone might not 
list anything — but the note even covers that: "no ':cloud' model in 
'ollama list' — the ollama tier will be skipped ('ollama signin' plus a 
cloud model enables it...)". "plus a cloud model" — ambiguous but 
acknowledges you need a model. The note's remedy doesn't tell users to 
`ollama pull <tag>:cloud`. Eh — within the skill's own docs there's 
tension: Step 4 says for cloud, "also run `ollama signin` once ... one 
click, free, no payment. For the local backup model, tell me your 
available RAM..." — nothing about pulling a cloud tag. If signin alone 
doesn't populate `ollama list` with a `:cloud` entry, auto-detect finds 
nothing even though the user is signed in and a cloud tier would work with 
an explicit tag. This is a RISK (breaks on normal user flow) — but I can't 
verify ollama's actual behavior; it's fiction-2026. The internal claim 
"the script auto-uses whichever one the signin provides" (onboarding.md 
Step 3) asserts signin provides/list it. I'll flag as UNVERIFIABLE or RISK 
with the caveat. Given I have no tooling and this is a fictional CLI, I'll 
put under UNVERIFIABLE: "whether `ollama list` exposes the signed-in cloud 
tag" — actually the diff's own comment claims it. The task says: note 
load-bearing claims that can't be checked from the text under 
UNVERIFIABLE. The detect assumption is load-bearing (the "runs 
automatically with no env var" claim). I'll flag it.

Issue 6: **`command -v ollama` guard means: ollama exists but user set 
OLLAMA_MODEL in config? No config exists — fine.

**gg) A genuine BUG: the check treats `:cloud` inside the awk program 
string?** The awk program `NR>1 {print $1}` — no. The grep `':cloud$'` — 
single-quoted string in the file: characters `:cloud$` preceded by `'`. 
Pattern `[:cloud]$`... wait the PATTERN's first alternative is 
`[A-Za-z0-9][A-Za-z0-9._-]*:cloud` — needs `:cloud` preceded by a word 
char. In `grep -m1 ':cloud$'`, before the colon is a `'`. no match. And 
`:cloud$`'s trailing `$` — irrelevant.

What about the case statement arms? `case ... in` — first lines not shown; 
the shown context:

```
 is_cloud_ollama_tag() {
   case ... in (not shown)
     *) return 1 ;;
   esac
 }
```

Hmm the visible context lines show only `*) return 1 ;;`. There may be an 
arm like `*:cloud)` — as computed, `*` before `:` — no match. But what 
about `*[Ee]xample:cloud)`? Fictional. Skip.

**hh) Now, the check script's PATTERN vs. the word "Antigravity"...** not 
in pattern. "gy"? no.

**What about `gpt-oss` in setup-guide — unscanned. fine.

**ii) BUG candidate — check scans `SKILL.md` which contains the strict 
review prompt; does that prompt contain e.g. "glm"? Unknown. Old check 
only scanned for `glm-*:cloud`; new check scans `gpt-[0-9]` etc. If 
SKILL.md contains (unseen regions) e.g. "codex exec prints model: gpt-..." 
— the old SKILL.md had "model: gpt-5.6-terra" in the ONBOARDING Step 5 
(which MOVED to onboarding.md and got scrubbed to `<name>`). Did any other 
hunk-region or unseen region keep a literal? The diff hunks cover the 
places we saw. Unseen regions could contain concrete names that now fail 
CI: e.g., SKILL.md's "strict prompt" section or tier descriptions. Can't 
verify → note under UNVERIFIABLE: "remaining SKILL.md/script content 
outside the hunks is assumed free of pattern matches; if any concrete tag 
survives elsewhere in the four scanned files, CI fails" — but wait, 
actually that's backwards: if a concrete tag survives, check FAILS the 
build — that's the guard working, though it'd be an unintended break. I 
could flag as RISK: "new pattern is broader; any pre-existing concrete 
mention outside the edited hunks now hard-fails CI" — but I can't see 
those regions. Hmm. The old script's own comments contained `e.g. "codex 
(~/.codex config: gpt-5.6-terra..."` — scrubbed. `CODEX_MODEL=gpt-5.6-sol` 
docs scrubbed. Are there other gpt- occurrences? The heredoc "e.g. 
CODEX_MODEL=<model-tag>" scrubbed. Plausibly complete. I'll list under 
UNVERIFIABLE.

**jj) Now — a REAL logic bug in the Independence-rule rewrite in 
SKILL.md:**

```
cross-model = Codex + ollama-cloud
(classified by the family of the tag actually used — the standard default
pair)
```

Hmm — "classified by the family of the tag actually used": the 
parenthetical is odd but OK — meaning the ollama-cloud seat's family 
depends on which cloud model the owner signed in with (e.g., if the 
owner's cloud model is a GPT-family... then codex+ollama-cloud might BOTH 
be same family as host/host?). The point: cross-model status of the ollama 
seat is no longer guaranteed GLM; it depends on the signed-in model. On a 
Claude host, Codex (gpt) + ollama-cloud (whatever) — if the owner signed 
in with a GLM model, fine. But if the owner's signed-in cloud tag is, say, 
a Gemini-family or Claude-family model, the "standard default pair" might 
have codex+ollama both non-cross... they're different families from each 
other presumably. The gate needs ≥1 cross-model reviewer; codex is 
cross-model for a Claude host regardless. For a Codex host, ollama-cloud 
must be non-OpenAI family — if the owner's signed-in cloud model IS 
gpt-family (e.g., gpt-oss:cloud?), the ollama seat fails the Independence 
rule exactly like Codex-host+Codex. The doc acknowledges: "(classified by 
the family of the tag actually used)". OK — consistent handling. Fine.

**kk) BUG candidate — `run_agy` return code / MODE check unchanged; `agy 
models` for detection unchanged. The header prints "CLI default model" — 
the trail/consolidation expects "model" field; closeout.md says label 
"tool, version, model, and sandbox mode" — "CLI default model" as the 
model label satisfies shape but hides actual model; onboarding Step 5 
requires confirming the model from the run's output. Minor consistency: 
the printed header now can't record which model ran. NIT: suggest 
capturing `agy`'s reported model from output if feasible, or instructing 
the consolidator to record it. Eh — the raw output itself (agy.out) 
presumably contains model info. NIT.

**ll) The env-doc table** (comment lines) for OLLAMA_MODEL — matches 
implementation ("auto-detected ... first ':cloud' tag in ollama list"). 
Fine.

**mm) Now check the ollama skip note logic versus `run_ollama`'s `return 
3`** (comment "must be named explicitly"). run_ollama requires 
OLLAMA_MODEL named — auto-detect sets it, so fine.

Wait, actually there's a subtlety: `run_ollama() { [ -n 
"${OLLAMA_MODEL:-}" ] || return 3 }` return 3 = "skipped tier (missing 
CLI/auth)". When auto-detect finds nothing, OLLAMA_MODEL empty → 
run_ollama return 3 → treated as skipped (not attempted) → the setup 
heredoc still prints (it prints regardless? it's in the usage section 
printed when...). The heredoc at 515 — "cat >&2 <<'EOF'" appears to print 
instructions when tiers are missing. Fine.

**nn) One more check-script edge**: `grep -nE "$PATTERN" "${FILES[@]}"` — 
if a scanned file contains the string `check_model_defaults.sh`? Not 
matching pattern. Contains "glm-[0-9]"? The new script's comment says 'A 
hardcoded default is exactly how the ollama tier once drifted' — no tags. 
Good.

But the CHECK script itself is in `scripts/`, not scanned. Fine.

**oo) `set -uo pipefail` in check script: `mismatches=$(grep ...)` — if 
grep fails due to... fine.

**pp) Makefile target doc updated; check target message updated. Fine.

**qq) GitHub workflow comment updated. Fine.

**rr) Now let me reconsider the removed "OLLAMA_MODEL default" semantics 
for `--local-only`:**

Old behavior: in local-only, OLLAMA_MODEL never defaulted; caller-supplied 
cloud tag refused. New: auto-detect guarded by `LOCAL_ONLY != 1`, so 
local-only never auto-detects (and auto-detect could only find `:cloud` 
anyway, which is refused). Consistent.

**ss) What about the previous behavior where ollama tier defaulted to a 
cloud tag even when ollama wasn't installed?** Old script: 
`OLLAMA_MODEL=glm-5.3-flash:cloud` always set (non-local-only) → 
run_ollama would invoke `ollama run glm-5.3-flash:cloud` (presumably 
checks `command -v ollama` → return 3 if absent). Same result. Fine.

**tt) Now — is the auto-detect `grep -m1 ':cloud$'` picking a LOCAL model 
mislabeled? Tag must end in `:cloud`, which per is_cloud_ollama_tag is 
cloud-shaped. OK.

**uu) The note goes to stderr — good, won't pollute review output. Fine.

**vv) Now the onboarding doc's agy section rewrite**: 
"`independent_review.sh` passes `--model "$AGY_MODEL"` only when that env 
var is set" — matches new script. Old SKILL.md said default "Gemini 3.1 
Pro (High)". setup-guide still says "typically a Gemini-family default 
from the Antigravity login". Consistent.

BUT — **setup-guide.md still says**: "the onboarding reference's canonical 
invocation (its Step 5)" — but onboarding.md Step 5 doesn't contain a full 
invocation; it describes `--model "$AGY_MODEL"` conditionally. The 
setup-guide now shows the invocation `AGY_MODEL="<a Claude model from 'agy 
models'>" agy --sandbox --model "$AGY_MODEL" -p` and claims it matches the 
onboarding reference's canonical invocation. The onboarding file doesn't 
literally show an invocation command (Step 5 text mentions 
`independent_review.sh` passes `--model ...` only when set). Minor drift; 
NIT.

**ww) Also setup-guide claims "matching the onboarding reference's 
canonical invocation (its Step 5) exactly"** — the invocation there is 
`agy --sandbox --model "$AGY_MODEL" -p` — matches the AGY_MODEL-set 
branch. OK-ish.

**xx) Another possible BUG: SKILL.md's reviewer-stack item 1 still says 
"Override per-run with `CODEX_MODEL=<model-tag>`" — fine.

**yy) Frontmatter: "the standard pair (Codex + your signed-in ollama-cloud 
model)" — good. "first use runs a guided onboarding wizard" — good.

**zz) IMPORTANT: the frontmatter no longer documents 
`--first-success`/`--local-only` semantics — body still does. fine.

Now let me also double-check the **check script vs. 
`references/setup-guide.md` file naming**: check comment says 
"references/setup-guide.md is deliberately NOT scanned". Fine.

**Cross-file pointer check — SKILL.md step 2 exclusion text now says 
"clerk item 3 (`references/closeout.md`) commits it"** — wait: "The trail 
must still be in the MR diff — a repo CI gate may require it and clerk 
item 3 (`references/closeout.md`) commits it". closeout.md item 3 = Trail. 
Good.

**SKILL.md line ~555 local-only text: "under `--local-only` the cloud 
auto-detect default is never applied, and an explicitly-set cloud tag is 
refused outright"** — matches script. Good.

**Now the Independence-rule "classified by the family of the tag actually 
used" — wait, there's a subtle wording bug**: "cross-model = Codex + 
ollama-cloud (classified by the family of the tag actually used — the 
standard default pair)". Previously "(GLM, Z.ai — the standard default 
pair)". The parenthetical now explains classification. OK.

**One more possible BUG: the fresh-eyes seat description changed from 
"Fable pass (Agent tool, model: "fable")" to "an extra pass with a 
stronger host-family model (the Agent tool's model option)". The check 
pattern wouldn't have caught "Fable" anyway; fine.

Also SKILL.md step ~726: "the no-shared-context fresh-eyes seat" (Fable 
removed) — good.

**Now — ranked findings. Let me consolidate:**

1. **RISK — check_model_defaults.sh PATTERN misses common concrete-name 
spellings**: `gpt-[0-9]` lowercase-only misses "GPT-5"/"Gpt-5"; `Claude[- 
][0-9]` misses "Claude Opus 4.6" / "Claude Sonnet" version-bearing names; 
misses "GLM-5" without... wait `[Gg][Ll][Mm]-[0-9]` catches glm-5. Misses 
"Gemini 3.1" — no wait, `[Gg]emini[- ][0-9]` catches "Gemini 3". Misses 
"Claude Opus 4.6", "Claude Haiku 4", "Gemini Pro" (no digit), 
"gpt-oss:cloud"? gpt-[0-9] misses "gpt-oss". So a future line "e.g. Claude 
Opus 4.6 (Thinking)" in SKILL.md passes the guard. Concrete fix: extend 
pattern (e.g. `[Cc]laude[^A-Za-z]*[0-9]|gpt-[A-Za-z0-9]|(?i)gpt-` etc. — 
GNU grep -E lacks case-insensitive-inline; use `grep -iE`? but that would 
make family names GLM/Gemini bare matched too — allowed categories would 
break). Simplest: add `[Cc]laude[ 
[A-Za-z]]*(Opus|Sonnet|Haiku)[^0-9]*[0-9]`? Or word-list approach. I'll 
say: extend `gpt-[0-9]` to `[Gg][Pp][Tt]-` and add `(Opus|Sonnet|Haiku)` 
and `gpt-oss`. This is a "guard that cannot fire" for a realistic class → 
RISK.

2. **RISK — frontmatter trigger-phrase regression**: dropped triggers 
("antigravity review", "agy review", "set up ollama for review", "set up 
independent review", etc.) though onboarding Step 6 and body still 
instruct users to use those exact phrases. A user saying "antigravity 
review" may not surface the skill on hosts keyed off the description. Fix: 
re-add the dropped phrases to the frontmatter list.

Wait, also "worth burning a antigravity credit" — body still has. The 
frontmatter is what triggers. Yes RISK.

3. **BUG? — auto-detect note doesn't fire when ollama is absent**: If 
`ollama` isn't installed, the condition `command -v ollama` fails → no 
note, and previously the tier would still be "attempted" with a default 
(failing loudly with the setup guidance). Now silently skipped. Is that a 
behavior regression? Let me think again about what old code did with 
ollama absent: OLLAMA_MODEL defaulted to glm-5.3-flash:cloud regardless of 
ollama presence (no command -v guard in old code). run_ollama: `[ -n 
OLLAMA_MODEL ]` passes → presumably next line checks `command -v ollama || 
return 3`. We don't see run_ollama's body. The visible first line: 
`run_ollama() { [ -n "${OLLAMA_MODEL:-}" ] || return 3  # must be named 
explicitly`. The subsequent body unknown — maybe it checks daemon 
reachable and prints setup help. Either way, absent-CLI → return 3 (skip) 
probably in both old and new. So the note gap is cosmetic → NIT: also 
print the note (different wording) when CLI missing, or fold into the 
skipped-tier summary so the reason surfaces ("no OLLAMA_MODEL and ollama 
CLI missing" vs "no cloud tag").

Actually, is there a subtler functional regression: previously a user with 
ollama installed, signed in, with cloud models listed, but OLLAMA_MODEL 
unset — old script would send to glm-5.3-flash:cloud; if that tag was 
retired, run fails → visible failure. New: picks their actual first cloud 
tag — the intended fix. Good.

4. **RISK — auto-detect depends on `ollama list` surfacing signed-in cloud 
models**; if signin alone doesn't list a `:cloud` tag (in real ollama, 
cloud models must be pulled/pushed to appear in `ollama list` — actually 
`ollama ps`? hmm), the standard second reviewer silently vanishes for 
users who followed onboarding ("a one-time ollama signin ... nothing 
beyond that"). The skill's own onboarding Step 3/4 tells users signin is 
sufficient; Step 6 says "cloud if it's available, local as a degraded 
fallback". If a user signed-in-but-nothing-pulled gets no `:cloud` entry 
in `ollama list`, the note fires telling them exactly that, so it's 
discoverable — it's a UX papercut not a hard bug. But there IS a contract 
mismatch: SKILL.md says "the script auto-detects your signed-in `:cloud` 
model from `ollama list` — runs automatically... with no env var needed", 
while onboarding's Step 4 ollama-cloud instruction only says run `ollama 
signin` — no step ensuring a `:cloud` tag EXISTS in `ollama list`. The 
note is the safety net. I'd rank this RISK (the "runs automatically with 
no flags once both are set up" promise depends on an unverified `ollama` 
behavior) — partially UNVERIFIABLE. I'll fold: RISK with fix = have 
onboarding/signin step verify `ollama list` shows a `:cloud` tag (Step 5's 
evidence standard already would catch it in test review). Actually Step 5 
test review would catch it. Hmm. The onboarding Step 5 requires a real 
test review quoting output — that covers it. So it's more NIT. Let me 
downgrade to NIT/RISK-lite... The instruction says RISK = "breaks on 
normal change, or a guard that cannot fire". The check for signin-only 
setups is Step 5. I'll list as UNVERIFIABLE + NIT.

5. **BUG candidate — `ollama list` parse when ollama prints a 
deprecation/notice line before the table**: speculative. skip.

6. **NIT — closeout.md duplicate content risk**: closeout.md duplicates 
the SKILL.md step-9 summary; SKILL.md's summary says "collision-proof 
naming rules there" — consistent. The big win is single source. CLEAN.

7. **NIT — check_model_defaults.sh echo block formatting**: The FAIL 
message uses multiple echo lines; fine.

8. **NIT — `PATTERN` also matches things like "2026-08-09:cloud"?** no.

Actually let me hunt for **false-positive** matches within the four 
scanned files that would break CI NOW. This is the most valuable check — 
would `bash scripts/check_model_defaults.sh` pass on this tree as diffed?

Scan SKILL.md (new version) line by line for pattern components:
- `[A-Za-z0-9][A-Za-z0-9._-]*:cloud`:
  Any occurrence of `:cloud` preceded by alnum/._-? New SKILL.md: "ollama 
cloud (`OLLAMA_MODEL`)" — no ':cloud'. "auto-detects your signed-in 
`:cloud` model" — backtick precedes. In code spans? The reviewer stack 
item 2: "the script auto-detects your signed-in `:cloud` model from 
`ollama list`." — `'`:cloud`'` preceded by backtick char — backtick is not 
in class. procedure --local-only: "the cloud auto-detect default is never 
applied". OK. What about `--local-only` paragraph: "an explicitly-set 
cloud tag is refused outright". fine.
  Trigger phrases in body: "ollama-cloud" no colon.
  Any "tier 2 ... :cloud"? In onboarding only (file 2).

- `[Gg][Ll][Mm]-[0-9]`: SKILL.md scrubbed GLM mentions? The frontmatter 
mentions no glm. Independence section changed. Any other GLM? "see the 
skill body" hmm — the diff only shows changed hunks; unchanged SKILL.md 
regions might still contain "GLM" bare (allowed without version) or `glm-` 
versioned (would fail!). Old SKILL.md had `glm-5.3-flash:cloud` in several 
places — the hunks we see cover reviewer stack item 2, --local-only para, 
Independence rule para. Was there any other? The old check required every 
`glm-*:cloud` mention to match canonical or have the marker — old SKILL.md 
passed, mentions: SKILL.md x4 per old header comment ("nine prose 
references (SKILL.md x4, the script itself x5)"). The diff hunks show: 
frontmatter (old frontmatter — did it contain glm? The removed frontmatter 
text: "Codex (gpt-class) + ollama-cloud (GLM)" — no tag). reviewer stack 
item 2 (had glm-5.3-flash:cloud + marker glm-5.2:cloud) ✔ hunk. 
Independence parens "(GLM, Z.ai...)" ✔ hunk. --local-only para ✔ hunk. 
That's 3... "SKILL.md x4" — the 4th: maybe in the Onboarding section 
(removed entirely, moved to onboarding.md: "e.g. the default 
`glm-5.3-flash:cloud`" in Step 2 — that's within the big removed chunk ✔). 
So SKILL.md glm mentions all removed. Good.

- `gpt-[0-9]`: old had `gpt-5.6-sol` (x2: SKILL.md reviewer stack + 
script), `gpt-5.6-terra` (script). All scrubbed. Any other gpt-N in 
unchanged SKILL.md? E.g., "strict prompt" or tier table? Unknown—can't 
verify. UNVERIFIABLE.

- `[Gg]emini[- ][0-9]`: old SKILL.md had "Gemini 3.1 Pro (High)" in 
reviewer stack item 4 (✔ hunk scrubbed) and onboarding Step 5 (moved & 
scrubbed ✔). Others? "Antigravity/Gemini" bare — allowed. "Gemini" bare 
elsewhere — allowed (no digit). Good.

- `[Cc]laude[- ][0-9]`: old SKILL.md "Claude Opus 4.6"? hmm — old text in 
reviewer stack item 3: "a **Fable** pass (Agent tool, `model: "fable"`) 
... on top of the Sonnet host pass" — "Sonnet" isn't matched by pattern 
(only Claude[- ][0-9]). "Sonnet" would survive... but the new text 
replaced it anyway. Wait — do any UNCHANGED regions of SKILL.md contain 
"Claude 4.x" or "Sonnet"? Pattern doesn't match "Sonnet" alone. "Claude 
Code" — "Claude C" no digit. OK. What about "claude-opus-4-6" style? Not 
visible.

onboarding.md scan: "Codex (~/.codex config" fine; `gpt-`? The Step 5 
text: 'e.g. a `model: <name>` line' scrubbed ✔. "glm-5.2:cloud"? The old 
Step 2 had "e.g. the default `glm-5.3-flash:cloud`" — scrubbed to bare 
`:cloud` ✔. Any "Gemini 3.1"? Old Step 5: "AGY_MODEL defaulting to "Gemini 
3.1 Pro (High)"" — scrubbed to "typically a Gemini-family default" ✔ and 
"Gemini-family default from the Antigravity login". `[Gg]emini[- ][0-9]` — 
"Gemini-family" → after "Gemini" comes "-f" not digit → no match ✔.

independent_review.sh scan: header, env doc, default block, run_codex 
comment, run_agy, heredoc — all scrubbed. Unchanged regions: 
`is_cloud_ollama_tag()` case arms (not visible; could contain `:cloud` 
preceded by quote/star). The visible context begins:

```
 is_cloud_ollama_tag() {
    case ... (not shown lines: presumably *)
    *) return 1 ;;
  esac
 }
```

Hmm the hunk shows:

```
 is_cloud_ollama_tag() {
+    ...
     *) return 1 ;;
   esac
 }
```

Wait actually the first hunk context lines:

```
@@ -104,12 +107,17 @@
 is_cloud_ollama_tag() {
     *) return 1 ;;
   esac
 }
-# The standard second reviewer is ...
```

Hmm, that means between `is_cloud_ollama_tag() {` and `*) return 1 ;;` 
there are unchanged lines not shown (grep context default 3 lines... two 
lines shown above the change). Actually hunk header `@@ -104,12 +107,17 
@@` with context starting mid-function. The case arms for cloud tags (e.g. 
`*[.:]cloud*) return 0 ;;`) sit in hidden lines. If an arm contains 
`:cloud` preceded by `*` (glob), fine. If `gl[oa]b:cloud` improbable. 
Could contain `[a-z]:cloud`? e.g. `*:cloud)` → char before `:` is `*` → 
safe. Likely fine.

Also `PROMPT_PORTABLE` (hidden) — likely generic. `looks_like_review()` 
hidden. Could contain example strings with model names? Unknowable. 
UNVERIFIABLE bullet.

closeout.md scan: contains "Codex" "agy" "ollama"? It mentions "codex" 
(CLIs), "local ollama"? Let me scan: it has tier mentions; "ollama" 
appears? The closeout text includes "run the script with --local-only ... 
tier-3 host fresh-eyes"? No — that's SKILL.md step 5 region. closeout.md 
includes clerk items — mentions "codex" only as reviewer names ("read-only 
sandboxes (codex)"). No pattern hits. ✔

So CI should pass. Good — no BUG of the "breaks immediately" kind, 
assuming hidden regions are clean.

**Now deeper BUG check on the ollama auto-detect + `looks_like_review` / 
header**:

`run_ollama` prints header presumably `## Independent review — ollama 
cloud (glm-5.3...)`? Hidden. If it hardcodes the old tag in a printf 
(unchanged region!), the check would flag it → CI fail now. Old script 
passed old check with canonical=glm-5.3-flash:cloud — a header printf 
containing the literal `glm-5.3-flash:cloud` is just a variable, unlikely 
literal. Fine.

**The ollama tier skip note's accuracy**: "the ollama tier will be 
skipped" — true since run_ollama returns 3 on empty. ✔

**Auto-detect chooses the FIRST ':cloud' tag** — if the owner has multiple 
cloud tags (e.g., old glm-5.2:cloud still pulled + new), first may be 
arbitrary (alphabetical in `ollama list`? ollama list sorts by... it lists 
in pull order? Actually `ollama list` output order is by model name? I 
recall it's unordered-ish (by creation). "first" is deterministic-ish for 
the owner but arbitrary otherwise. The doc says "your first ':cloud' tag" 
— fine. NIT: if owner has both an obsolete and current cloud tag, 
auto-detect may pick the obsolete one silently; previously the default was 
curated. That's actually the RISK the new design accepts ("whatever the 
owner signed in with IS the default"). Could add: prefer newest by 
MODIFIED column. NIT.

**One more possible BUG**: In `--first-success` mode the ollama seat skip 
note prints even when codex succeeds? The note prints at startup (before 
running tiers), regardless of --first-success. If codex succeeds first, 
the note about ollama skip is harmless noise. Fine.

**Windows note in onboarding**: PowerShell equivalent retained. Fine.

**Now, the missing check on `references/setup-guide.md`**: the check 
deliberately excludes it, but SKILL.md's onboarding now directs users to 
setup-guide for concrete tags. Guard's scoping is documented. But here's a 
subtle RISK: the exclusion comment says setup-guide "cites dated, concrete 
local-model examples for the RAM table, and says so inline" — the diff 
added that inline caveat ("dated examples for the LOCAL sanity tier only — 
the review pipeline itself is model-agnostic and hardcodes none of them"). 
OK consistent.

But — **RISK: the guard's FILES list will silently NOT scan `references/` 
files added in the future** (e.g., a new reference file) — the check only 
fails on files in its list. A new reference with a concrete tag passes CI. 
That's inherent scoping; the missing-file guard helps renames but not 
additions. Could flip default (scan all of skill dir except allowlist). 
That's a design NIT/RISK. The old check had the same property (scanned 2 
files). I'd mention as RISK: guard cannot fire for new pipeline files not 
yet listed; fix = scan directory glob minus explicit allowlist 
(setup-guide). Actually the skill dir also contains the strict prompt etc. 
Glob `"$SKILL_DIR"/**` minus setup-guide + maybe other exempted refs. 
Recommend. RISK.

**Also: check scans only `skills/independent-review` — SKILL.md 
frontmatter description is what other skills/docs may duplicate? e.g., the 
repo website might quote "glm-..." — out of scope of stated purpose. Fine.

**Now the `closeout.md` numbering check**: It says "Numbered 'point N' / 
'step N' references refer to SKILL.md's Procedure section." Within 
closeout: references "step 6(b)'s cap", "step 9's permission table" wait — 
hold on. closeout.md's permission table rows reference "9(b), clerk items 
1–2", "9(a)", "clerk item 3", "clerk item 2". And text references "step 
9's own 'not applicable is not failure' rule", "Procedure step 6", 
"Procedure step 5" (in onboarding). In SKILL.md, the close-out is step 9 
and the verification-round cap is step 6(b) per existing text ("these 
re-gates do not count against step 6(b)'s cap") — the original SKILL.md 
said the same. Fine, can't fully verify — but the moved text is verbatim, 
so internal consistency holds as long as SKILL.md's Procedure kept 
numbering. SKILL.md Procedure steps 1–8 presumably intact (we see steps 
2,3 (tier3/run),4 consolidate,6,7?,9 edited). The big deletions were 
inside step 9 itself and the Onboarding section. So numbering intact. ✔

**One real wording bug in closeout.md cleanup item**: "delete the run's 
`$RAW_DIR` ... it held the full artifact content plus every reviewer's raw 
output (owner-only permissions, but proprietary code sitting in `$TMPDIR` 
indefinitely serves no purpose...)" — this is the same text as before 
minus the lead-in? Old: "delete the run's `$RAW_DIR` (path printed to 
stderr as `raw output: <dir>`)" — appears in the fallback sentence. Same 
both. ✔ moved verbatim. Note the moved text loses the em-dash context: 
fine.

**Check SKILL.md's new step 9 summary vs closeout details**: "read it from 
the TOP (the list is ranked — a tail hides the BUGs)" ✔ matches closeout. 
"post each reviewer's raw notes plus the consolidated verdict to the PR/MR 
with the SHA-stamped marker before merging" ✔.

**SKILL.md reference "(`references/closeout.md`)" in step-2 and step-5 
(paste tier) hunks ✔.**

**Does SKILL.md still reference the deleted wizard content anywhere?** 
e.g., step descriptions referencing "Step 4b RAM detection" or 
"setup-guide" in Procedure? There's a hunk at SKILL.md ~268 
(--local-only). Others unseen. Possible stale pointer: "the permission 
table below" in SKILL.md step-5 hunk was updated to point to closeout. ✔ 
visible. Other unseen instances would now dangle → would be a BUG if 
exist; can't verify → UNVERIFIABLE.

Also the old SKILL.md Onboarding section had the Windows/PowerShell 
details and the "three checks" — all moved. New SKILL.md keeps the "guided 
ONBOARDING" mention in frontmatter ✔ description says "first use runs a 
guided onboarding wizard".

**Frontmatter trigger list vs onboarding Step 6 quote**: Step 6 says "see 
the trigger phrases in this skill's frontmatter" — frontmatter still lists 
several (codex review, gemini review...). "antigravity review" is in Step 
6's plain-language examples but NOT in trimmed frontmatter → inconsistent; 
onboarding tells users phrases that may no longer trigger the skill. 
Reinforces finding #2. Merge into it.

**Now — the `make check` / CI message consistency**: workflow name changed 
✔.

**Anything about `set -uo pipefail` + `cd` in check script**: `cd 
"$(dirname "$0")/.."` — if cd fails (dir renamed), `set -u`... without -e, 
cd failure continues and greps run from wrong cwd → grep fails (file not 
found) with exit 2 → command substitution → mismatches empty → prints OK — 
FALSE PASS when run from wrong dir! Wait: `cd "$(dirname "$0")/.."` — $0 
is the script path; dirname works if invoked normally. But if cd fails 
(e.g., invoked via symlink? dirname of symlinked path...). Add `|| exit 
1`. Old script had same `cd` (unchanged line — shown as context). So 
pre-existing. But note FILES check would catch wrong-cwd? No — if cd 
failed, `$SKILL_DIR/...` relative from wrong cwd → file missing → FAIL 
(loud). Actually good: the existence check turns silent-pass into 
loud-fail. So cd-fail yields FAIL due to missing FILES. OK fine — the new 
FILES guard incidentally hardens that. Nice, mention under CLEAN.

**Now `ollama list` under `set -euo pipefail`?** independent_review.sh's 
set options unknown (head not shown beyond line 47 context). If script has 
`set -e`: the assignment `OLLAMA_MODEL="$(ollama list 2>/dev/null | awk 
... | grep -m1 ':cloud$' || true)"` — the `|| true` is inside the 
substitution, applied to the pipeline → pipeline's failure masked → 
substitution succeeds → assignment rc 0 → no -e exit. ✔. And `[ -n 
"$OLLAMA_MODEL" ] || echo ...` guarded. ✔ The `if` condition with `command 
-v` under -e safe. ✔

**grep -m1 exit 1 with no match → substitution yields "" but rc masked by 
|| true ✔.**

Wait, actually there's a pipefail subtlety: `set -o pipefail` + grep -m1 
exiting 1 while awk still... `|| true` covers pipeline failure. ✔

**Edge: `awk 'NR>1 {print $1}'` — if `ollama list` prints a notice like 
"Couldn't connect..." to stdout with 2>/dev/null only catching stderr → 
awk outputs field1 "Couldn't" → grep ':cloud' fails → empty ✔.**

Alright.

**Another item: SKILL.md reviewer stack item 2 says "auto-detects your 
signed-in `:cloud` model from `ollama list`. The skill prescribes no 
specific model — set `OLLAMA_MODEL` to pick a different cloud or local 
tag" ✔ consistent with script.**

**The env-doc in script says OLLAMA_MODEL "(auto-detected) defaults to the 
first ':cloud' tag in `ollama list`" ✔.**

**The usage heredoc says "`ollama signin` — your first ':cloud' tag 
auto-serves as the default" ✔.**

Hmm wait — subtle: after `ollama signin`, does the user have any cloud tag 
locally? The fiction asserts yes. UNVERIFIABLE (external CLI behavior). 
Note it.

**Now the antigravity change — one functional nuance**: previously 
`run_agy` ALWAYS passed `--model "$model"` with default "Gemini 3.1 Pro 
(High)"; the trail labeled "antigravity/agy (Gemini 3.1 Pro (High), 
sandbox)". Now label "CLI default model" — the consolidated PR comment 
labels model as "CLI default model", which violates closeout's own 
instruction to label comments with "tool, version, model, and sandbox 
mode" meaningfully (closeout item 1: "clearly labeled with tool, version, 
model, and sandbox mode"). "CLI default model" is not a model. The fix: 
the run output (agy.out) may report the model; or instruct consolidator to 
record the model from run output; or print a warning if model not 
confirmable. Also onboarding Step 5 REQUIRES confirming the model from run 
output before calling agy gate-satisfying. The header could at least say 
"(CLI default model — confirm from output)". NIT/RISK-lite. I'll list NIT 
with fix suggestion.

Also **AGY_MODEL empty-string vs unset**: `agy --sandbox -p` (no --model) 
— the doc says passing --model only when set ✔. But note: previously, on a 
Gemini host, gate-satisfying required overriding AGY_MODEL; unchanged 
semantics ✔.

One more: onboarding says "`independent_review.sh` passes `--model 
"$AGY_MODEL"` only when that env var is set — the var is this script's own 
convention, mapped straight onto `agy`'s real `--model` flag" ✔ matches.

setup-guide: `AGY_MODEL="<a Claude model from 'agy models'>" agy --sandbox 
--model "$AGY_MODEL" -p` ✔ matches set-branch.

**setup-guide review-quality row: "(Gemini family)" ✔.**

**setup-guide "Verify" date 2026-07-02 vs old SKILL claims of 2026-08-28 
for glm tag — removed. fine.**

**Now the onboarding Step 3 ollama-cloud bullet**: "*Cloud* (any 
`:cloud`-tagged model — the script auto-uses whichever one the signin 
provides)" — slight overclaim: script auto-uses the FIRST one in ollama 
list, not "whichever one the signin provides" (same thing if one). 
NIT-level; skip or fold.

**Check the Makefile/CI text vs actual check name**: script still named 
check_model_defaults.sh (name now slightly stale — "model defaults" but it 
enforces absence of defaults). NIT: rename e.g. check_model_agnostic.sh; 
keep filename to avoid churn — mention. NIT.

**Ranking**:

BUG: Do I have any real "wrong now"? Candidates:
- None that hard-fail or silently misbehave *now* with certainty… unless 
hidden regions have pattern hits (unverifiable, would be a CI break = BUG 
if true — report as UNVERIFIABLE, not BUG).

Hmm wait — actually, let me reconsider one thing: the check script pattern 
includes `[A-Za-z0-9][A-Za-z0-9._-]*:cloud`. SKILL.md has: "the script 
requires the EFFECTIVE ollama tag to be local" ... "--local-only the cloud 
auto-detect default is never applied" — no `:cloud`. But wait — new 
SKILL.md reviewer stack: "auto-detects your signed-in `:cloud` model from 
`ollama list`." ✔ safe.

And — hold on, `onboarding.md` line: "ollama CLOUD (a signed-in 
`:cloud`-tagged model)" ✔.

What about `SKILL.md` line: "(a signed-in `:cloud`-tagged model, e.g. the 
default `glm-5.3-flash:cloud`)" — wait, was that ONLY in the removed 
Onboarding Step 2? The removed chunk includes: "ollama CLOUD (a signed-in 
`:cloud`-tagged model, e.g. the default `glm-5.3-flash:cloud`) DOES count, 
same as Codex/Antigravity — it's tier 2..." — this is inside the big 
deleted wizard block (Onboarding section). The replacement onboarding.md 
Step 2 scrubbed the e.g. ✔. And the REMAINING SKILL.md Independence-rule 
section — the old text (pre-diff) around "Per host: **Claude Code** — 
fresh-eyes = the Claude pass (optionally a Fable pass...)" was changed. 
Any other `glm-` in remaining SKILL.md? The strict-prompt section? 
Unknown. Old check counted SKILL.md x4 glm mentions: frontmatter? no. (a) 
reviewer stack (2 tags: default + old tag w/ marker), (b) onboarding step 
2, (c) --local-only, (d) independence parens? "(GLM, Z.ai...)" isn't 
`glm-*:cloud` — it's bare "GLM" — hmm the old PATTERN was 
`glm-[A-Za-z0-9.-]+:cloud` — "(GLM, Z.ai)" doesn't match. So the 4 were: 
reviewer stack (2 lines = 2 mentions?), count "SKILL.md x4" = 4 lines: 
reviewer-stack default line, reviewer-stack old-tag line, onboarding e.g. 
line, --local-only line. All four are in hunks and scrubbed ✔. 

And script x5: header default line, OLLAMA_MODEL env line, the assignment 
line, run_ollama header?, heredoc. We see header/env/assignment/heredoc 
scrubbed (4). 5th: maybe the run_ollama section comment "# must be named 
explicitly" — or a line in run_ollama like `printf '## ... (%s...)' 
"$OLLAMA_MODEL"` (no literal). Where's the 5th? Possibly in 
`PROMPT_PORTABLE` or usage "ollama signin for the glm-5.3-flash:cloud 
default tag" ✔ heredoc. Hmm: counted hunks: (1) top header comment — old 
line had `glm-5.3-flash:cloud` ✔ scrubbed; (2) env block OLLAMA_MODEL ✔ 
scrubbed; (3) the default assignment ✔ removed; (4) usage heredoc ✔ 
scrubbed. That's 4. 5th unseen — could be in run_ollama or in the strict 
prompt construction or in `looks_like_review()`. If the 5th literal 
remains in an unchanged region → check FAILS CI right now → BUG (build 
broken). Can't verify from diff → UNVERIFIABLE with high stakes; flag 
prominently: "if the fifth historical glm mention the old header counted 
('the script itself x5') sits in an unshown region, the new check fails 
immediately". I'll place under UNVERIFIABLE (load-bearing) and ALSO as a 
RISK? The instruction: UNVERIFIABLE only for ones that matter — this 
matters a lot. I'll list it there prominently.

Actually, similarly SKILL.md had "SKILL.md x4" counted and I found 4. For 
the script "x5" I can only point at 4 scrubbed. One unaccounted. Good 
catch to surface.

Also — "nine prose references (SKILL.md x4, the script itself x5)" — total 
9; the old check's PATTERN `glm-...:cloud` would catch `glm-5.2:cloud` too 
(it matches glm-[A-Za-z0-9.-]+:cloud!) — the old-tag line had the marker 
to suppress. So mentions included the old tag. Fine.

So the 5 script mentions might include comment lines referencing the old 
tag; hunks scrubbed 4; 5th unknown → maybe in run_ollama header printf or 
in `check_model` ... or maybe in a comment about --local-only ("the 
glm-5.3-flash:cloud default is never applied" — in the script around the 
local-only guard: the diff's context shows the local-only comment block: 
"Skipping the DEFAULT above isn't enough..." — the OLD text before it: "-# 
NOT defaulted in --local-only mode: ... glm-5.3-flash:cloud is a network 
call by definition —" — that's the OLD removed line (counted). New 
replaced ✔. So hunks account for: header(1), env(2), assignment(3), 
local-only comment(4), heredoc(5)? Wait the local-only comment old line 
contained `glm-5.3-flash:cloud` — the removed lines in hunk 2: `-# env var 
needed to get the default duo...` hmm hunk 3's removed line: "# NOT 
defaulted in --local-only mode: ... glm-5.3-flash:cloud is a network call 
by definition —". Yes! So 5 accounted: header, env-doc, assignment(default 
line), local-only comment, heredoc. All scrubbed ✔✔. Then script clean. 
SKILL.md 4 accounted ✔. So probably CI passes. Good — downgrade my worry 
to "verify hidden regions" as a lighter UNVERIFIABLE item.

Wait also old SKILL.md reviewer stack had BOTH `glm-5.3-flash:cloud` 
(default line) and `glm-5.2:cloud` (old-tag line w/ marker) — that's 2 
lines in reviewer stack + onboarding e.g. + --local-only = 4 ✔ consistent 
with "x4". And both scrubbed. And `glm-5.2:cloud` matches new PATTERN 
`[Gg][Ll][Mm]-[0-9]` ✔ would fail if left — it's removed ✔.

OK so now — any remaining real *BUG*? Let me re-examine the auto-detect 
condition ordering:

```bash
if [ "$LOCAL_ONLY" != "1" ] && [ -z "${OLLAMA_MODEL:-}" ] && command -v 
ollama >/dev/null 2>&1; then
```

`LOCAL_ONLY` variable set earlier presumably (0/1). With `set -u`, if 
LOCAL_ONLY unset → error. It's presumably set from arg parsing before this 
point (old line referenced it too: `[ "$LOCAL_ONLY" = "1" ]`). ✔.

**What if `ollama` exists but `ollama list` hangs/times out** (daemon 
start on demand)? `ollama list` with server not running: newer ollama 
auto-starts server (takes seconds). Blocking at source-time. Minor; old 
code didn't call `ollama list` at all. NIT: could add timeout. Eh — I'll 
mention briefly.

**Another possible BUG**: The note text — "the ollama tier will be 
skipped" — but run_ollama's `return 3` semantics mean skipped; fine.

**Now, `--first-success` interplay**: unchanged.

**Now think about `check_model_defaults.sh` grep on binary-ish?** No.

**One more genuine issue — the check's FAIL message lacks the "how to fix" 
guidance** (old had "Update the mismatched reference(s)..."). New message 
says name families/generic shapes. Fine.

**Marker concept removed**: old check had 'non-default mention' marker 
exclusion; new check has NO inline exclusion mechanism. setup-guide 
exclusion is by file list. So if someone wants to discuss a concrete model 
with the marker, no escape hatch → they'd have to reword. The docs now 
scrub everything anyway. Fine — but onboarding.md Step 4b says "recommend 
exactly ONE model tag" from setup-guide — that's fine since setup-guide 
holds tags.

Hmm — but **onboarding.md instructs `ollama pull <model>`** — generic. ✔

**Now: SKILL.md deleted the "trigger phrases" frontmatter details 
including "set up independent review"**; frontmatter keeps "set up AI code 
review". OK.

**Double-check the frontmatter YAML validity**: it's a folded block 
`description: >` with lines — fine. Contains `--with-antigravity` with 
leading dashes — inside block scalar, fine.

**Makefile comment line length** fine.

**Now let me also verify no claim mismatch**: script comment says "Codex 
and Antigravity use their own CLI-configured defaults" (workflow comment) 
✔ agy now uses CLI default ✔ codex from config.toml ✔.

**SKILL.md stack item 1**: "model + effort from `~/.codex/config.toml`" ✔ 
unchanged.

**One inconsistency**: workflow/comment says "the ollama tier auto-detects 
the owner's signed-in ':cloud' model" ✔.

Now NIT list:

- NIT: `ollama list` | awk | grep — if the owner has multiple `:cloud` 
tags, first is arbitrary and may be an obsolete pull; sort by MODIFIED 
(last column) or warn. Anchor: scripts/independent_review.sh:114-ish (the 
awk line).
- NIT: no note when ollama CLI absent (silent tier skip differs from the 
CLI-present-no-cloud case). Fix: move note outside the command -v 
condition and word it to cover both.
- NIT: label "CLI default model" in agy header conflicts with closeout's 
"labeled with ... model" requirement — suggest extracting model from 
output or labeling "(CLI default — model unconfirmed)". Anchor: run_agy 
printf.
- NIT: onboarding Step 3 wording "the script auto-uses whichever one the 
signin provides" vs implementation "first ':cloud' tag in ollama list" — 
reword.
- NIT: script filename check_model_defaults.sh now enforces absence of 
defaults — rename optional.
- NIT: setup-guide invocation cross-ref says "matching the onboarding 
reference's canonical invocation (its Step 5) exactly" but onboarding no 
longer shows a literal invocation command — soften.

RISK list:

- RISK: guard can't fire for pipeline files not in FILES list (future 
reference files); invert to directory scan with explicit allowlist. 
Anchor: check_model_defaults.sh FILES=(...).
- RISK: pattern misses common concrete-name spellings: lowercase-only 
`gpt-[0-9]` (misses "GPT-5"/"gpt-oss"), `Claude` family only when digit 
follows "Claude-" (misses "Claude Opus 4.6", "Sonnet"). Fix: add 
case-variant alternations + (Opus|Sonnet|Haiku). Anchor: PATTERN line.
   Hmm wait: "Claude Opus" — family name without version allowed per 
comment ("Family names WITHOUT a version (Gemini, GLM, gpt-class, Claude) 
stay allowed"). "Claude Opus 4.6" contains a version → violates intent → 
pattern misses it. Yes RISK.
   Also `gpt-[0-9]` — "gpt-oss" is a local tag (allowed? it's concrete but 
local; the guard's intent is "named cloud tags" + "versioned family/model 
names" — gpt-oss:cloud would be caught via :cloud; bare gpt-oss maybe 
fine). But "gpt-5" uppercase "GPT-5" plausible in docs. Also misses 
"o3"/"o4" models, "Qwen..."? Not needed. Also misses `GPT-5`… fix pattern.
- RISK: frontmatter trigger regression (normal user phrasing no longer 
matches) — actually this is arguably a RISK (breaks on normal use) more 
than NIT. The frontmatter IS the skill's activation surface. Dropping 
"antigravity review"/"agy review"/"set up ollama for review"/"install 
codex for review" while body/onboarding still promise those phrases 
triggers a discoverability break. I'd rank RISK high (it's a normal, 
expected usage change). Fix: restore the list (cheap).

- RISK: auto-detect silently changes which model reviews as the owner 
pulls/signs in across time — the trail records "ollama-cloud (<tag>)" each 
run so auditability OK. But a stale/obsolete cloud tag first-in-list could 
silently downgrade review quality; the old curated default prevented 
exactly this ("the ollama-cloud default drifted out of sync" is the 
incident motivating this whole change — the new design replaces drift with 
auto-pick). Wait, actually the incident was doc/default DRIFT, not bad 
pick. The new approach can still pick a stale tag. Mitigate: note in 
output which tag was chosen (run_ollama header presumably prints model ✔). 
I'd keep as NIT.

- RISK: `command -v ollama` gate on auto-detect — if ollama IS installed 
but not on PATH of this shell (GUI install), tier skips with note only if 
command -v fails → no note (covered by NIT above).

BUG candidates final: none concrete. Should I really return zero BUGs? 
What about the **check pattern matching `:cloud` in `ollama list` help 
text inside setup-guide** — unscanned file ✔. In onboarding? none.

Hmm — wait, one more: **SKILL.md frontmatter description lost the 
"PLAN-typed artifacts DEFAULT to ≥2 reviewers attempted / --first-success 
honored" nuance** — that's frontmatter (activation/trust copy). Body 
retains it? The body's "Reviewer stack" mentions --first-success in header 
comment of script ("or the first that succeeds with --first-success" ✔). 
Body Procedure step... The old frontmatter explicitly documented it; new 
doesn't. NIT.

Also frontmatter lost the explicit statement "Antigravity/Gemini is OPT-IN 
ONLY ... owner's credits are scarce" — kept shortened ✔ ("its credits are 
scarce").

Also dropped from frontmatter: "same-family Fable pass on request" → 
generalized ✔ consistent with body.

**Check "closeout.md" heading says "(Procedure step 9)" ✔ and step 9 in 
SKILL.md points to it ✔ — cross-ref "clerk item 3 
(`references/closeout.md`) commits it" in step 2 ✔; "step 9's permission 
table" references INSIDE closeout.md refer to "step 9" which is now split 
across SKILL.md (the step) and closeout.md (the table). closeout.md says 
"step 9's permission table" meaning its own table (it IS the step-9 
content). But closeout's header says "Numbered 'point N'/'step N' 
references refer to SKILL.md's Procedure section" — yet the table row says 
"clerk items 1–2 | POST AUTHORITY" and text says "step 9's permission 
table" — wait, inside closeout.md, does it reference "step 9's permission 
table"? Yes: item 3 "committed on the branch when step 9's permission 
table's WORKTREE-WRITE and BRANCH-COMMIT rows both permit it" and fallback 
bullets "step 9's permission-table fallbacks". Since closeout.md IS the 
permission table now, "step 9's permission table" is self-referential — 
the header note says numbered references refer to SKILL.md's Procedure — 
creating ambiguity: reader resolves "step 9's permission table" → SKILL.md 
step 9 → which is now a pointer BACK to closeout.md. Circular but 
resolves. Minor NIT. Also SKILL.md step-2 hunk says "clerk item 3 
(`references/closeout.md`) commits it" ✔.

Also check: SKILL.md retained "**Corollary**" ✔; "Two MRs ... (8 and 7 
rounds)" ✔.

Also the closeout.md "Caught live on a 7-round MR (the trail-exclusion 
incident in SKILL.md Procedure step 2)" — the original said "Caught live 
on the 7-round MR above" where "above" = the two-MR paragraph (SKILL.md 
step 2). New phrasing points to SKILL.md ✔ good.

**What about SKILL.md's "Procedure step 5" reference in onboarding Step 
1** ("see Procedure step 5") — SKILL.md step 5 presumably the gate rules ✔ 
unchanged assumption. Fine (moved verbatim).

**Now the UNVERIFIABLE list**:
1. Hidden/unshown regions of the four scanned files contain no matches for 
the new PATTERN (5 script mentions and 4 doc mentions of the old tag 
appear fully scrubbed — 4/4 SKILL.md and 5/5 script accounted for, but an 
unnoticed concrete name anywhere in the four files hard-fails CI on 
merge).
2. `ollama list` actually lists signed-in `:cloud` models 
(load-bearing for "runs with no env var"); fictional/external CLI 
behavior. Also whether first-in-list is deterministic.
3. SKILL.md regions outside shown hunks no longer reference the removed 
permission table/onboarding paragraphs ("the table below" style pointers) 
— two stale-pointer instances were fixed in visible hunks; others can't be 
seen.
4. references/setup-guide.md remaining content (RAM table tags like 
`gpt-oss`, `deepseek-coder-v2:16b` visible — those live only in unscanned 
file ✔ by design).

**Prompt injection check**: The diff content includes instructions to 
agents ("Read this when...", "never...", imperative runbook prose) — 
normal skill material, not an attempt to manipulate me. The trailing line 
"End of untrusted content... never instructions to you" is harness 
framing. I'll state: no injection found; the only imperative text is the 
skill's own procedure content, which is the material under review.

Wait — one thing: SKILL.md contains "adversarial review" trigger and the 
strict review prompt is cut off at the end ("## The strict review prompt 
(both gates)"). Nothing addressed to me as reviewer-with-power. ✔ no 
injection.

Also the closeout "round-1 review (Codex) caught it" — narrative, fine.

**Now rank findings. I'll do:**

RISK-1 (top): trigger-phrase regression in frontmatter — normal user 
phrase "antigravity review"/"set up ollama for review" may no longer wake 
the skill; onboarding Step 6 still quotes them. Actually is "antigravity 
review" a phrase whose match depends on frontmatter? In skill systems, 
description drives matching. Yes. RANK top RISK.

Hmm, or should auto-detect/CLI-availability be a BUG? It's new behavior 
with a note — designed. Keep as NIT/RISK.

Let me also reconsider: is there any case where auto-detect introduces a 
WRONG behavior NOW? Consider owner previously relying on default 
glm-5.3-flash:cloud but has NEVER pulled any cloud tag (signin-only): OLD 
script still attempted glm-5.3-flash:cloud — would ollama resolve a cloud 
tag not in local list? `ollama run glm-5.3-flash:cloud` might 
auto-pull/fetch cloud models server-side (fiction). OLD behavior: works 
without any local listing. NEW: requires a `:cloud` entry in `ollama list` 
— **if cloud models don't appear in `ollama list` until pulled, then a 
signed-in user whose default previously "just worked" now silently loses 
the ollama seat** (note printed, tier skipped). That's a plausible real 
behavior break NOW — but it hinges on the fictional CLI's semantics which 
the diff asserts ("auto-detects your signed-in ':cloud' model from `ollama 
list`"). The diff itself claims the signed-in model shows in ollama list. 
Within the diff's world, consistent. Given the old script's comment "the 
owner's signed-in ollama-cloud model" and old default worked without 
listing, the new detect is a behavior CHANGE with a failure mode (silently 
fewer reviewers) — I'd rank this as RISK #1 or #2: "detect requires the 
tag to be locally listed; signin-only users (the path onboarding teaches) 
may have none → standard pair silently becomes single-reviewer; fix: fall 
back to attempting `ollama signin`-provided default via `ollama run <tag>` 
probe, or make the note fatal/summary-visible, or document `ollama pull` 
in Step 4". Also the note IS printed, so not fully silent. Still RISK.

Also: does the note even print when daemon absent but CLI present? `ollama 
list` fails (daemon down) → note prints misleading "no ':cloud' model in 
'ollama list'" — actually fine-ish.

OK final ranked list:

1. RISK — check_model_defaults.sh:20 (FILES) — guard cannot fire for any 
pipeline file not hand-listed (new/renamed reference files pass silently); 
only missing listed files fail. Fix: scan `skills/independent-review` 
recursively, subtract an explicit allowlist (setup-guide.md, maybe SKILL 
subpaths), fail on unlisted-but-present files or at least include 
references/*.md minus setup-guide.

2. RISK — check_model_defaults.sh:26 PATTERN — misses common spellings: 
uppercase `GPT-5`, `gpt-oss`, `Claude Opus 4.6`/`Sonnet 4` (digit not 
adjacent to "Claude"), so the exact failure class it polices can still 
creep in. Fix: broaden: `[Gg][Pp][Tt]-[A-Za-z0-9]`, `[Cc]laude[- 
][0-9]|[Cc]laude.*(Opus|Sonnet|Haiku)` etc.

3. RISK — scripts/independent_review.sh:~112 auto-detect — ollama tier now 
silently drops for signin-only users if `ollama list` doesn't show 
signed-in cloud tags (or `ollama` missing → no note at all); standard pair 
degrades to solo Codex with only a one-line stderr note that's easy to 
miss in a review round. Fix: cover CLI-absent case too, and/or verify 
detect assumption (see UNVERIFIABLE), and record the skip in the run 
summary, not just a startup note.

4. NIT — independent_review.sh run_agy header prints "CLI default model": 
closeout item 1 demands comments labeled with model; relabel "(CLI default 
— confirm from output)" or parse the model from agy.out.

5. NIT — auto-detect picks first matching tag, which can be an obsolete 
pull; prefer newest by MODIFIED column or echo chosen tag. anchor the awk 
line.

6. NIT — onboarding.md Step 3 "auto-uses whichever one the signin 
provides" vs actual "first ':cloud' tag in ollama list" — align wording. 
anchor onboarding.md Step 3 cloud bullet.

7. NIT — setup-guide.md "~181" claims to match "onboarding reference's 
canonical invocation (its Step 5) exactly" but onboarding shows no literal 
invocation — soften to "the invocation shape used in Step 5".

8. NIT — script/check name check_model_defaults.sh + old CI step name 
remnants: name says defaults; it now enforces the absence of defaults (doc 
updated, name stale). Optionally rename; harmless.

Hmm also NIT: frontmatter dropped the "≥2 reviewers attempted for PLAN 
artifacts / --first-schema honored" summary — body presumably retains; low 
value. Maybe skip to keep list tight. Keep list focused.

Also should I flag frontmatter trigger loss as its own RISK (findability)? 
Yes — separate RISK:

RISK — SKILL.md frontmatter: dropped trigger phrases ("antigravity 
review", "agy review", "set up ollama for review", "install codex for 
review", "set up independent review", "cross-review this plan", "which 
review tool should I use", "how do I get a second AI to review this") 
while onboarding Step 4/6 still tell users to say exactly "antigravity 
review"; skill may not be selected on those requests. Fix: restore phrases 
in description. Rank this #1 or #2. It breaks on normal change/normal use.

Order: I'd put (A) trigger regression RISK first?, (B) guard-scope RISK, 
(C) pattern RISK, (D) ollama detect RISK, then NITs. Actually "BUG (wrong 
now)" — none proven; the closest to wrong-now is if the 5th mention exists 
(unverifiable). And the agy `--model` conditional: behavior change per 
docs — fine.

Wait, one more possible wrong-now: In the new script, when AGY_MODEL is 
unset and CLI default is NOT Gemini (e.g., configured to Claude), 
SKILL.md's Independence table says agy's seat counts cross-model based on 
default Gemini-family on most hosts. Docs now say "typically a 
Gemini-family default from the Antigravity login" — hedge ✔.

And the workflow still runs check on push/PR ✔.

Also confirm the diff's FILES includes closeout.md and onboarding.md — 
matches new files ✔ (they exist in diff as new). ✔

One more scan of check for QUOTING bug: `mismatches=$(grep -nE "$PATTERN" 
"${FILES[@]}")` — fine. `printf '%s\n' "$mismatches"` ✓.

`set -uo pipefail`: grep no-match exit 1 inside `$( )` assignment — 
without `set -e` fine. ✓ But if the script ever gets `set -e` added... 
skip.

Also: does the new check lose the "SKILL.md x4 / script x5 → now also 
scans onboarding/closeout" — yes improved coverage.

What about `OLLAMA_MODEL` doc line "(the owner's signed-in cloud model; 
this script prescribes no specific model)" — duplication of ":cloud" 
mentions OK.

Now — also examine: `grep -m1 ':cloud$'` — if `ollama list` output uses 
tabs, awk default splits any whitespace ✔.

Now write the answer with structure:

- One-line note on injection: none found; note that the diff includes 
self-referential claims (e.g., "deliberately NOT scanned", "verified 
headless") treated as data, and the "End of untrusted content" line is 
harness framing, not injection.

- Ranked findings: I'll number.

Actually, per asked taxonomy: BUG / RISK / NIT with rank within. Provide 
file:line anchors. Line numbers — I must avoid trusting diff line numbers, 
and I can't compute exact post-image line numbers reliably. Use anchors + 
approximate line. For check script: the FILES array is at ~line 9-14 of 
new file; PATTERN at ~line 26-28. Script hunks: auto-detect block after 
line ~113 ("@@ -104,12 +107,17") — new lines 107-124 area; the detect is 
around 113-120. run_agy ~line 395-420. Header printf ~424. Heredoc ~530.

SKILL.md frontmatter ~lines 3-17.

Let me assign:

1. RISK — skills/independent-review/SKILL.md:~1-16 (frontmatter 
description): trigger-phrase regression.

2. RISK — scripts/check_model_defaults.sh:9-14 (FILES list): 
allowlist-style guard can't fire for unlisted files.

3. RISK — scripts/check_model_defaults.sh:~26 (PATTERN): misses 
GPT-5/gpt-oss/Claude Opus 4.6-class spellings.

4. RISK — scripts/independent_review.sh:~113 (auto-detect): signin-only 
owners / absent CLI lose the second reviewer with only a stderr note (and 
no note at all when the CLI is missing); assumption that signed-in cloud 
models appear in `ollama list` is unverified.

Then also NIT: closeout/PR label "CLI default model"; NIT: first-tag pick; 
NIT: setup-guide cross-ref wording; NIT: name.

Maybe merge 2-3 as guard weaknesses but keep separate as asked ("RANKED 
findings").

Clean list (explicit):
- check script: file-existence guard prevents silent no-scan / wrong-cwd 
silent pass; `grep -m1 ... || true` keeps pipefail-clean; no `set -e` 
needed on grep no-match; pattern quoted; missing-file FAIL message 
includes remediation.
- script: `|| true` inside command substitution prevents `set -e`/pipefail 
exit on no cloud tag; `command -v` guard order (local-only first) 
preserves local-only refusal semantics; caller-supplied cloud OLLAMA_MODEL 
still refused under --local-only (unchanged block) — the local-only note's 
new wording ("every ':cloud' tag is a network call") matches; run_agy 
returns 3 when agy absent unchanged; both agy branches capture rc and 
write same RAW files so downstream (raw capture / looks_like_review) 
unchanged; header printf still labels tool+mode.
- Move of step 9/clerk to closeout.md: the two in-SKILL references were 
re-pointed (step 5 paste-tier, step 2 clerk item 3); closeout text carried 
over verbatim including marker format, bound-at-two-attempts, prose-only 
rules, cleanup conditions — I compared the moved blocks against the 
deleted ones; they match (no dropped clauses spotted) — mention I diffed 
them mentally: the closeout.md content appears verbatim-equal to the 
deleted SKILL.md text including the "2026-08-09" codification and the 
marker literal `<!-- independent-review:consolidated sha=... -->` intact.
- SKILL.md remaining prose (independence table families without versions) 
conforms to new guard; glm-5.3/glm-5.2 mentions all removed from scanned 
files (accounted 4 in SKILL.md, 5 in script — matches old header's "nine 
references" count, which itself is a hint the scrub is complete).
- No remaining "Fable"/"Sonnet"/"gpt-5.6*" strings in visible scanned 
regions.
- AGY_MODEL change consistent across script env doc, run_agy, SKILL.md 
stack item 4, onboarding Step 5, setup-guide; setup-guide RAM-table caveat 
added inline as the check's comment claims.
- Workflow/Makefile text updates match script behavior.

UNVERIFIABLE:
- Unhunked regions of the four scanned files contain no remaining concrete 
tag (scrub count matches old "nine mentions" arithmetic — 4+5 — but I can 
only see hunks).
- External-CLI facts: does `ollama list` list signed-in ':cloud' models 
without an explicit pull; ordering of output; whether `agy` accepts 
invocation without `--model`; whether `codex exec` prints "model:" line. 
All load-bearing for auto-detect/Step 5.
- SKILL.md references to "step 9/step 5/step 6(b)" unchanged numbering 
outside hunks (can't see full Procedure).
- website-review cross-ref ("--first-success") still consistent in 
frontmatter removal (frontmatter no longer mentions it — body claim 
unseen).

I keep UNVERIFIABLE short: only ones that matter.

Also the injection report: "No prompt-injection content found; the diff is 
procedure prose + code. The final line '(End of untrusted content…)' is 
harness context, not diff content."

Line anchors — pick:
- SKILL.md frontmatter: `skills/independent-review/SKILL.md` description 
block (lines ~3–17).
- check script FILES: lines ~9–14; PATTERN: line ~26–27.
- script auto-detect: lines ~113–116 (the new `if [ "$LOCAL_ONLY"...` 
block).
- agy label: the final printf in run_agy (~line 424).
- awk line: ~114.
- setup-guide invocation: ~line 184-188 region (hunk @@ -180,12).
- onboarding Step 3 cloud bullet (new file ~line 100).

I should hedge exact numbers ("approx"). The instructions: "each with 
file:line or anchor". Anchor + approx line fine.

Also one more potential BUG check: `make check` target — runs both 
scripts; check_clean.sh might scan for credentials and now... not touched.

Also: does the new check need `grep -l`-style binary safety for 
onboarding.md emoji (🧑 🤖)? UTF-8 fine.

One more careful look at the guard's `:cloud` pattern and the phrase in 
onboarding.md: "(any `:cloud`-tagged model — the script auto-uses 
whichever one the signin provides)" — inside onboarding.md ✔ preceded by 
backtick. And closeout.md has none. SKILL.md "your signed-in ollama-cloud 
model" ✔.

Also the workflow file comment: "the owner's own CLI config/signin choose 
the models (see scripts/check_model_defaults.sh)" ✔.

Now the ranked list ordering. Severity reasoning:

- The trigger-phrase regression: normal usage breaks ("antigravity review" 
no longer guaranteed to surface skill). High.
- Guard FILES scoping: guard cannot fire for whole new files — core 
purpose. High.
- PATTERN gaps: same class, lower (some concrete names still caught via 
:cloud).
- ollama auto-detect degradation: plausible environment-dependent break; 
medium. Also note behavior change: previously default worked with zero 
local cloud tags (hardcoded tag attempted); now requires listing. That's 
arguably a BUG-if-real but unverifiable → RISK.

NITs: agy label; first-tag; setup-guide wording; script name; onboarding 
wording mismatch.

Also maybe NIT: check FAIL message now multi-line echo w/o trailing 
guidance of how to fix a legit citation need (e.g., trail files citing 
model tags are under docs/reviews — unscanned ✔ by design since only 
pipeline files scanned).

Wait — one thing: SKILL.md contains trail NAME examples 
`docs/reviews/REVIEW-diff-2026-08-09-r7.md` — fine.

Also SKILL.md step-2 hunk: "clerk item 3 (`references/closeout.md`) 
commits it" ✔.

Also check SKILL.md "the permission table (`references/closeout.md`) 
records an owner instruction (atom B: quoted verbatim)" ✔ present in 
closeout.

Write the final answer now. Keep it tight: ranked findings each with 
anchor, one-line why, concrete fix. Then CLEAN list. Then UNVERIFIABLE. 
Then injection note.

Format: headline per finding with severity rank. Use approximate line 
numbers noted as approx. Be explicit I have no tools; verified only 
against diff text.

Length: this is a dense review; aim for thorough but not bloated. Markdown 
with sections and numbered list. I'll number findings 1-8 with tags.
...done thinking.

# Review — independent-review "model-agnostic" refactor

Reviewed against the diff text only. No files read, nothing run; 
post-image line numbers are approximations computed from hunk headers.

## Prompt injection

None found. The diff contains runbook/procedure prose with imperatives 
aimed at the skill's runtime agent ("do not dump install commands", "never 
re-onboard"), which is ordinary material for this artifact, plus the 
harness framing line at the end. Nothing attempts to alter this reviewer's 
task, output, or conclusions.

## Ranked findings

1. **RISK — `scripts/independent_review.sh:~113` (new auto-detect block) — 
the standard pair can silently become a solo Codex run, and the change is 
stricter than the old default in one real case.** The old code attempted a 
hardcoded cloud tag even when `ollama list` showed nothing (empty local 
store, daemon freshly signed in, CLI present but daemon slow to start). 
The new code only auto-detects if a `:cloud` tag appears in `ollama list`; 
otherwise the ollama seat is skipped with a one-line stderr note that is 
easy to lose in a review round. Worse, when the CLI is *absent* the 
`command -v` guard short-circuits and **no note prints at all**. Fix: emit 
the note regardless of CLI presence (two distinct wordings), and surface 
ollama-skip in the end-of-run summary that already lists attempted tiers — 
or have onboarding Step 4's ollama-cloud instruction end with "verify a 
`:cloud` tag shows in `ollama list`."

2. **RISK — `skills/independent-review/SKILL.md:3–17` (frontmatter 
`description`) — trigger-phrase regression: the edit deletes more than 
half the trigger list, including phrases the surviving body still tells 
users to say.** "antigravity review", "agy review", "set up ollama for 
review", "set up independent review", "install codex/antigravity for 
review", "which review tool should I use", "how do I get a second AI to 
review this", "cross-review this plan" are all gone, while onboarding.md 
Step 4/6 (🧑 "run `agy models`"…; Step 6 "…*antigravity review,* …") still 
quotes those exact phrases and points at "the trigger phrases in this 
skill's frontmatter". On a description-keyed host, requests that 
onboarding itself teaches may no longer select the skill. Fix: restore the 
deleted phrases verbatim to the frontmatter list (the model-agnostic goal 
is untouched by doing so).

3. **RISK — `scripts/check_model_defaults.sh:9–14` (`FILES=( … )`) — the 
guard cannot fire for any pipeline file it wasn't told about.** Only the 
four listed files are scanned, and only their *absence* fails; a new 
`references/*.md` or a second script added to the skill is silently 
unscanned, so concrete tags can enter the pipeline with CI green. Fix the 
default direction: glob `"$SKILL_DIR"/**` (or at least `references/*`) and 
subtract an explicit allowlist (`references/setup-guide.md`, documented in 
the comment), failing on unrecognized new files or at least scanning them.

4. **RISK — `scripts/check_model_defaults.sh:~26–27` (`PATTERN`) — the 
regex misses common spellings of exactly the names it exists to ban.** 
`gpt-[0-9]` is lowercase-only, so `GPT-5` passes; `gpt-oss` passes (fine 
locally, but it will also travel to a cloud tag uncaught as `…:cloud` only 
via the colon arm); `Claude[- ][0-9]` doesn't match `Claude Opus 4.6` / 
`Sonnet 4` — the version sits one word away from the family name, so a 
future "use Claude Opus 4.6 for a hard case" line in SKILL.md passes CI. 
Fix: add `(Opus|Sonnet|Haiku)` and case-insensitive `gpt-` alternates to 
`PATTERN` (e.g. `[Gg][Pp][Tt]-`, `[Cc]laude[- ][^ ]*[- ][0-9]`).

5. **NIT — `scripts/independent_review.sh:~114` (`awk 'NR>1 {print $1}' | 
grep -m1 ':cloud$'`)** — "first `:cloud` tag in `ollama list`" is 
arbitrary: an owner who still has an obsolete cloud tag pulled (the exact 
drift this change cites as its origin incident) silently gets the stale 
one as the default second reviewer. Fix: prefer the newest row (`ollama 
list` prints MODIFIED as the last column) or echo the chosen tag loudly at 
run start so the trail captures it.

6. **NIT — `scripts/independent_review.sh` (final `printf` in `run_agy`, 
~line 424)** — label `"CLI default model"` violates this repo's own 
closeout rule that PR labels carry "tool, version, model, and sandbox 
mode", and gives the consolidator nothing to record. Fix: attempt to 
extract the reported model from `$RAW_DIR/agy.out` for the header, else 
print `"(CLI default — model unconfirmed; verify per onboarding Step 5)"` 
so the unconfirmed state is visible in the artifact.

7. **NIT — `skills/independent-review/references/setup-guide.md:~184`** — 
"matching the onboarding reference's canonical invocation (its Step 5) 
exactly" overstates: onboarding Step 5 no longer contains a literal 
invocation command, only a description of the `--model 
"$AGY_MODEL"`-when-set behavior. Fix: "…the invocation shape described in 
onboarding Step 5" or paste the command there.

8. **NIT — `scripts/check_model_defaults.sh` / `Makefile` / workflow step 
names** — the filename and the residual `check_model_defaults.sh` 
references now contradict the script's purpose (it enforces the *absence* 
of defaults). Harmless but confusing at handoff. Fix: rename to 
`check_model_agnostic.sh` and update the three references, or leave and 
accept the stale name.

## Checked and CLEAN

- **Scrub completeness for the old tags**: the old check's header claimed 
nine hand-maintained mentions (SKILL.md ×4, script ×5). The hunk 
arithmetic accounts for exactly 4 in SKILL.md (reviewer-stack default, 
reviewer-stack old-tag w/ marker, onboarding wizard's e.g., `--local-only` 
paragraph) and 5 in the script (header, `OLLAMA_MODEL` env doc, the 
default assignment, the `--local-only` comment, the usage heredoc); every 
one is deleted or genericized in a hunk. No `glm-<digit>`, `gpt-<digit>`, 
`Gemini <digit>`, or `<word>:cloud` match remains in any *visible* line of 
the four files — including inside onboarding.md and closeout.md, which 
arrived as verbatim moves and had to be scrubbed independently; they were.
- **check script hardening**: the new per-file existence guard 
incidentally prevents the old failure mode where a wrong `cd` / missing 
file would produce a silent clean pass (old script would grep zero files 
and print OK); `|| true` inside the auto-detect substitution keeps 
`pipefail`/`set -e` safe on the no-match path; both agy invocation 
branches write the same `$RAW_DIR/agy.out`/`agy.err` and share rc 
handling, so downstream raw-capture/`looks_like_review()` behavior is 
unchanged when `AGY_MODEL` is unset.
- **Semantics preserved by the move**: closeout.md's permission table, 
atom A/B rules, fail-closed properties, marker literal `<!-- 
independent-review:consolidated sha=… -->`, two-attempt bound, prose-only 
classification, and cleanup conditions match the deleted SKILL.md text 
clause-for-clause (I compared the moved blocks; nothing dropped, including 
the 2026-08-09 codification and the "no atom B for GATED-THIS-DIFF" rule). 
The two in-SKILL cross-references to the table were repointed to 
`references/closeout.md`.
- **Doc/code agreement on the new behavior**: SKILL.md reviewer-stack item 
2, script env doc, usage heredoc, workflow comment, and Makefile all 
consistently describe first-`:cloud`-tag auto-detection; the 
`--local-only` refusal text ("cloud auto-detect default is never applied… 
explicitly-set cloud tag refused") matches the still-present guard logic; 
`AGY_MODEL`-only-when-set is consistent across script, SKILL.md, 
onboarding Step 5, and setup-guide.
- **Independence-rule rewording**: "classified by the family of the tag 
actually used" correctly tracks the fact that the ollama seat is no longer 
a fixed GLM family; per-host tables in SKILL.md/onboarding agree with each 
other.
- **setup-guide stays unscanned as claimed**: its concrete tags 
(`gpt-oss`, `deepseek-coder-v2:16b` visible in context) sit only in the 
deliberately excluded file, and the diff added the inline "dated examples, 
LOCAL sanity tier only" caveat that the check's comment promises.

## UNVERIFIABLE (load-bearing)

- **No concrete tag survives in unhunked regions of the four scanned 
files.** The arithmetic above strongly suggests the scrub is complete, but 
I can only see changed hunks; a stray `GPT-5`/`Claude Opus` in an 
untouched paragraph would fail CI immediately on first run. One cheap run 
of the script settles it.
- **`ollama list` enumerates signed-in cloud tags without a pull.** The 
whole "runs automatically with no env var" claim, and finding 1's 
severity, rest on this external-CLI behavior; if signin-only owners have 
empty listings, the standard pair silently degrades by default.
- **`agy` accepts `--sandbox -p` without `--model`, and `codex exec` 
prints a `model:` line.** Both are now relied on by the script/onboarding 
and were previously only "verified" against older, differently-shaped 
invocations.

